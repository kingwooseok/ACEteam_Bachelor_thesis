# Raspberry Pi 5 macb/GEM Native XDP 포팅 및 런타임 검증

## 1. 문서 개요

이 문서는 Raspberry Pi 5의 온보드 Ethernet controller인 Cadence GEM/macb 드라이버에 native XDP 지원을 추가하고, PREEMPT_RT 기반 mixed-criticality packet processing 구조와 연결하기 위해 수행한 kernel-side porting, BPF application 수정, build/deployment, runtime verification 과정을 정리한 기술 문서다.

프로젝트의 전체 목적은 하나의 embedded Ethernet ingress에서 packet-level classification을 수행한 뒤 traffic의 요구사항에 따라 서로 다른 execution path로 분기하는 것이다.

현재 시험용 classifier는 다음과 같이 동작한다.

```text
UDP destination port 9001
    -> CPUMAP
    -> CPU 3
    -> Linux kernel network stack

UDP destination port 9002
    -> XSKMAP
    -> AF_XDP userspace

그 외 traffic
    -> XDP_PASS
    -> Linux network stack
```

9001/9002는 실제 차량 protocol을 의미하는 것이 아니라 각 execution path를 기능적으로 분리하기 위한 실험용 rule이다.

최종 구조는 다음과 같다.

```text
                         Raspberry Pi 5
                    Embedded Linux + PREEMPT_RT
                              |
                       Cadence GEM/macb
                              |
                         NIC RX DMA
                              |
                         page_pool RX
                              |
                        native XDP
                              |
                    packet-level classifier
                  /              |              \
                 /               |               \
                v                v                v
             AF_XDP           CPUMAP           XDP_PASS
            userspace          CPU 3          normal stack
                |                |                 |
                v                v                 v
              UMEM          kernel stack      UDP/socket
```

현재 기능 검증이 완료된 것은 이 datapath가 실제 장치에서 정상적으로 동작하는지에 대한 검증이며, deterministic behavior 자체에 대한 성능 결론은 별도의 latency/load measurement가 필요하다.

---

## 2. 저장소와 작업공간 분리

Application/BPF와 kernel source는 서로 다른 workspace에서 관리한다.

```text
PROJECT_ROOT
~/ACEteam_Bachelor_thesis

KERNEL_ROOT
~/rt-experiment/linux-6.18-rt
```

PROJECT_ROOT에는 다음을 둔다.

```text
BPF/
    xdp_kern.c
    xdp_loader.c
    afxdp_recv.c
    config.h
    Makefile

udp_socket/
    sender/receiver baseline

README/documentation
```

Kernel 전체 source는 team repository에 넣지 않고 KERNEL_ROOT의 별도 kernel Git tree에서 관리한다. Kernel branch는 `thesis-macb-xdp`이고 base는 Raspberry Pi Linux의 `rpi-6.18.y`이다.

현재 kernel branch의 작업 series는 다음 7개 commit으로 구성되어 있다.

```text
c520b905a460  net: macb: move Rx buffers alloc from link up to open
aa19c8495b99  net: macb: rename rx_skbuff into rx_buff
b9ae9a455248  net: macb: Add page pool support handle multi-descriptor rx frames
862b05223b8c  net: macb: make macb_tx_skb generic
8bf757177434  net: macb: generalize tx buffer handling
9088efa70019  net: macb: add XDP support for gem
f934dc812d68  net: macb: introduce ndo_xdp_xmit support
```

`rpi-6.18.y..thesis-macb-xdp` 기준 driver 변경량은 다음과 같다.

```text
drivers/net/ethernet/cadence/Kconfig       1 line
drivers/net/ethernet/cadence/macb.h       43 line changes
drivers/net/ethernet/cadence/macb_main.c  977 line changes

총 +793 / -228
```

---

### 왜 Generic XDP가 아니라 Native XDP여야 했는가

이번 프로젝트에서 Generic XDP를 Native XDP로 전환한 가장 중요한 이유는 단순한 처리속도 차이가 아니다. 최종 연구에서는 **NIC가 RX descriptor를 처리하는 시점의 hardware RX timestamp를 packet의 시작점으로 사용하고, 그 packet을 CPUMAP 또는 XSKMAP으로 steering한 뒤 downstream execution path에서도 그 정보의 연결관계를 유지하는 것**이 필요하다.

Generic XDP에서는 packet이 일반적인 SKB/network receive context에 들어온 뒤 XDP가 실행된다. 따라서 driver가 NIC RX descriptor에서 얻은 hardware metadata와 XDP packet context를 driver-level에서 직접 연결해 유지하기 어렵다.

연구가 목표로 하는 구조는 다음과 같다.

```text
NIC RX descriptor
        |
        +-- HW RX timestamp
        |
        v
   macb native RX
        |
        v
      xdp_buff
        |
        v
     XDP program
       /     \
      /       \
 CPUMAP       XSKMAP
    |             |
    v             v
 CPU 3        AF_XDP/UMEM
```

즉 Native XDP의 필요성은 "더 빠른 XDP를 쓰기 위해서"만이 아니라, **NIC RX 시점에서 얻은 metadata와 XDP packet을 driver의 실제 RX memory lifecycle 안에서 연결하기 위해서**다.

다만 현재 구현에서 HW RX timestamp를 XDP metadata callback까지 연결하는 작업은 아직 별도의 후속 단계다. 현재 macb에는 기존 SKB용 hardware timestamp 경로가 있고, 다음 단계에서 그 source를 `bpf_xdp_metadata_rx_timestamp()`로 연결할 예정이다.

따라서 아래 두 항목은 구분한다.

```text
현재 완료:
NIC RX -> native XDP -> CPUMAP / XSKMAP

후속 목표:
NIC HW RX timestamp
    ->
XDP metadata
    ->
CPUMAP / AF_XDP
    ->
userspace measurement
```

이 구분을 명확히 하는 것은 현재 문서에서 "native XDP가 동작한다"는 사실과 "timestamp metadata measurement까지 완성되었다"는 사실을 혼동하지 않기 위해 중요하다.

# 3. 왜 macb driver 자체를 수정했는가

Generic XDP는 driver가 native XDP를 지원하지 않아도 사용할 수 있지만, packet이 SKB로 변환된 뒤 XDP가 실행된다.

이번 연구가 필요한 것은 다음과 같은 native execution point다.

```text
NIC
 |
 +-- DMA RX buffer
 |
 +-- macb RX processing
 |
 +-- XDP
 |
 +-- verdict
       +-- PASS
       +-- DROP
       +-- REDIRECT
       +-- TX
```

즉 XDP program이 기존 network stack보다 앞선 driver RX 경로에서 실행되어야 한다.

기존 macb RX는 preallocated SKB 중심 lifecycle이었다.

```text
netdev_alloc_skb()
    ->
dma_map_single()
    ->
rx_skbuff[]
    ->
RX descriptor
```

수신 후:

```text
RX descriptor
    ->
rx_skbuff[]
    ->
skb_put()
    ->
dma_unmap_single()
    ->
eth_type_trans()
    ->
checksum
    ->
gem_ptp_do_rxstamp()
    ->
napi_gro_receive()
```

형태다.

이 구조에서는 XDP buffer ownership, XDP RXQ, page-pool memory model을 직접 연결하기 어렵다. 따라서 XDP를 먼저 붙이는 대신 RX memory lifecycle을 먼저 XDP-compatible 형태로 바꾸는 것이 필요했다.

---

## 4. Upstream patch를 그대로 적용하지 않고 manual porting한 이유

macb XDP/page-pool implementation은 upstream net-next의 patch series를 reference로 사용했다.

처음에는 patch를 `git am`으로 현재 Raspberry Pi tree에 직접 적용하려 했다. 그러나 v3 series의 초기 patch부터 context mismatch가 발생했다.

이후 v6 7-patch series를 사용했지만 3/7 patch에서 다음과 같은 3-way merge 실패가 발생했다.

```text
sha1 information is lacking or useless
drivers/net/ethernet/cadence/macb.h
could not build fake ancestor
```

repository가 shallow clone인지 확인했고 전체 history를 fetch하는 절차도 수행했지만, patch가 필요로 하는 과거 `macb.h` blob이 현재 Raspberry Pi repository의 object database에 존재하지 않았다.

따라서 단순 shallow clone 문제만으로 설명되지 않았고, patch base와 현재 Raspberry Pi tree의 역사적 연결이 직접 성립하지 않는 것으로 판단했다.

결과적으로 다음 방식을 택했다.

```text
upstream patch series
        |
        | reference / design source
        v
현재 Raspberry Pi macb source
        |
        v
manual porting
        |
        v
각 commit별 build/runtime 검증
```

이렇게 하면 현재 Raspberry Pi tree의 구조와 PREEMPT_RT 수정사항을 유지하면서 upstream implementation의 필요한 설계를 이식할 수 있다.

---

# 5. Step 1 — RX buffer allocation lifecycle

### 먼저 쉽게 이해하기

이 단계가 하는 일은 "packet을 처리한다"가 아니라, **packet을 받을 준비를 언제 하고, RX ring과 buffer를 언제 만들고 언제 정리할지를 정돈하는 것**이다.

Ethernet driver는 NIC에게 "여기에 다음 packet을 써라"라고 미리 memory를 알려줘야 한다. 그래서 RX ring에는 항상 사용 가능한 buffer가 어느 정도 준비되어 있어야 한다.

기존 lifecycle에서는 link 상태 변화와 RX buffer allocation의 관계가 복잡하게 얽혀 있었다. XDP를 붙이려면 앞으로 buffer가 SKB가 될 수도 있고 XDP buffer로 남을 수도 있으므로, **buffer의 생명주기를 명확하게 분리하고 allocation 실패도 호출자에게 알려줄 수 있어야 했다.**

쉽게 말하면:

```text
기존:
링크가 올라오거나 내려갈 때
    ->
RX buffer를 만들고 없애는 흐름이 같이 움직임

변경:
device open / recovery 등 명확한 lifecycle
    ->
RX ring과 buffer를 준비
    ->
오류가 나면 위 단계까지 정확히 보고
```

즉 이 단계는 뒤의 XDP 코드를 직접 추가한 단계가 아니지만, **XDP가 사용할 RX memory를 예측 가능하게 만드는 준비 단계**다.



첫 번째 patch는 RX buffer allocation과 ring initialization의 lifecycle을 정리하는 것이다.

주요 변화는 다음과 같다.

```text
gem_rx_refill()
    void -> int

gem_init_rx_ring()
    void -> int

gem_init_rings()
    -> int + fail_early
```

`mog_init_rings()` 호출 규약에도 `fail_early`가 추가되었다.

RX buffer allocation과 ring initialization은 기존 link-up 경로 중심에서 device open lifecycle 쪽으로 이동했다.

이 변경은 link flap 상황에서 필요하지 않은 RX ring 재할당/해제를 줄이고, 다음 page-pool porting을 위한 명확한 buffer lifecycle을 만든다.

또한 allocation 오류를 호출자까지 전달할 수 있으므로 초기화 실패를 조용히 넘기지 않는다.

---

# 6. Step 2 — rx_skbuff에서 rx_buff로 추상화

### 먼저 쉽게 이해하기

`rx_skbuff`라는 이름 자체에 이미 "이 RX buffer는 SKB다"라는 가정이 들어 있다.

하지만 XDP에서는 packet이 SKB가 되기 전에 XDP가 먼저 실행될 수 있다. 따라서 RX slot의 내용물을 무조건 `struct sk_buff *`라고 생각하면 안 된다.

```text
기존
rx_skbuff[]
    =
SKB pointer array

변경
rx_buff[]
    =
generic buffer pointer array
```

즉 이름을 바꾼 이유는 단순히 코드가 보기 좋아서가 아니라, **RX memory를 SKB에 종속되지 않게 만들기 위해서**다.

이것이 바로 다음 page pool 단계와 XDP buffer 처리의 출발점이다.



기존:

```c
struct sk_buff **rx_skbuff;
```

를:

```c
void **rx_buff;
```

로 변경했다.

이는 단순한 변수명 변경이 아니다.

기존 구조는 RX ring에 항상 `struct sk_buff *`가 들어간다는 전제를 갖고 있었지만, page pool 이후에는 packet buffer의 실제 memory representation을 더 일반적으로 취급해야 한다.

이 abstraction은 다음 Step에서:

```text
page_pool_alloc_frag()
        ->
buffer address
        ->
rx_buff[]
```

형태를 사용할 수 있게 한다.

---

# 7. Step 3 — page pool 및 multi-descriptor RX

### 먼저 쉽게 이해하기

여기가 실제로 RX memory model이 크게 바뀌는 단계다.

Page Pool은 쉽게 말해 **네트워크 RX에서 반복적으로 사용할 page memory를 효율적으로 재사용하기 위한 pool**이다. 매 packet마다 일반적인 방식으로 memory를 새로 잡고 버리는 대신, page를 pool에 보관하면서 NIC/CPU/network stack 사이에서 반복해서 돌려쓴다.

왜 필요한가?

XDP는 packet을 아주 앞단에서 처리하기 때문에 "이 memory를 누가 소유하고 있는가"가 중요하다.

예를 들어:

```text
NIC가 packet을 쓰는 중
    ->
CPU/XDP가 packet을 검사
    ->
PASS면 SKB가 이 buffer를 사용
DROP이면 page pool로 반환
REDIRECT면 다른 XDP path가 소유
TX면 다시 NIC TX가 소유
```

처럼 하나의 buffer가 상황에 따라 다른 경로로 넘어간다.

따라서 page pool을 중심으로 memory ownership과 recycle을 관리하는 것이 XDP 도입에 적합하다.

또 packet 하나가 RX descriptor 하나에 들어간다는 보장도 없다. 큰 frame은 여러 descriptor로 나뉠 수 있기 때문에:

```text
descriptor 1
    +
descriptor 2
    +
descriptor 3
    ->
하나의 packet
```

으로 다시 조립할 수 있어야 한다.

그래서 `napi_build_skb()`와 `skb_add_rx_frag()`를 사용해 single-descriptor와 multi-descriptor frame을 모두 처리한다.

이 단계가 끝나면 driver는 **SKB allocator 중심의 RX driver에서 page-pool memory를 재사용하면서 XDP가 접근할 수 있는 RX memory를 관리하는 driver**로 바뀐다.



이번 porting에서 가장 큰 RX-side 변경이다.

Kconfig에서:

```text
CONFIG_MACB
    selects PAGE_POOL
```

관계를 추가했다.

`struct macb_queue`에는:

```text
struct page_pool *page_pool
struct sk_buff *skb
```

를 추가했고 `struct macb`에는:

```text
rx_headroom
rx_buffer_size
rx_ip_align
```

등 RX layout 관련 정보를 추가했다.

## 7.1 기존 방식과 변경 후 방식

기존:

```text
netdev_alloc_skb()
    ->
dma_map_single()
    ->
rx_skbuff[]
```

변경:

```text
page_pool_alloc_frag()
    ->
page_pool DMA address
    ->
rx_buff[]
    ->
napi_build_skb()
```

Page Pool을 사용하면 page allocation/recycling과 DMA ownership을 driver가 일관된 방식으로 관리할 수 있다.

`gem_rx_refill()`에서는:

```text
page_pool_alloc_frag()
    ->
page_pool_get_dma_addr()
    ->
gem_rx_pad() 반영
    ->
dma_sync_single_for_device()
    ->
RX descriptor
```

의 순서로 buffer를 device가 사용할 수 있도록 준비한다.

## 7.2 multi-descriptor frame

기존 단일 SKB 중심 RX path에서 여러 descriptor를 이용하는 frame까지 처리하도록 변경했다.

```text
first descriptor
    ->
napi_build_skb()

subsequent descriptor
    ->
skb_add_rx_frag()

last descriptor
    ->
checksum / timestamp
    ->
napi_gro_receive()
```

따라서 jumbo/multi-descriptor frame도 하나의 SKB 형태로 재구성할 수 있다.

## 7.3 buffer ownership

정상 RX:

```text
Page Pool
    ->
NIC DMA
    ->
CPU
    ->
napi_build_skb()
    ->
network stack
    ->
skb_mark_for_recycle()
    ->
Page Pool
```

drop/error:

```text
Page Pool
    ->
XDP / error
    ->
page_pool_put_full_page()
```

형태로 ownership이 다시 page pool 쪽으로 돌아간다.

이 단계는 XDP 자체를 넣기 전에 일반 Linux networking regression을 먼저 확인하는 것이 중요했다.

---

# 8. Step 4 — TX buffer abstraction

### 먼저 쉽게 이해하기

이제 RX가 어느 정도 XDP를 받을 준비가 되었으니 TX 쪽도 바꿔야 한다.

기존 TX ring은 사실상:

```text
TX buffer
    = SKB
```

라는 전제로 만들어져 있다.

하지만 XDP에서는:

```text
SKB
XDP_TX frame
외부에서 들어온 XDP frame
```

을 같은 TX ring infrastructure에서 다룰 필요가 있다.

그래서 `tx_skb`를 `tx_buff`로 일반화했다.

즉 이 단계의 목적은:

> "TX ring은 SKB만 보내는 곳이 아니라 packet buffer를 보내는 공통 통로다."

라고 driver의 자료구조와 코드가 이해하도록 만드는 것이다.



기존:

```text
struct macb_tx_skb
```

를:

```text
struct macb_tx_buff
```

로 일반화했다.

이 단계의 목적은 TX ring metadata가 오직 SKB만 담는다는 가정에서 벗어나는 것이다.

이후 XDP_TX와 ndo_xdp_xmit에서 `struct xdp_frame`을 같은 TX infrastructure에 태울 수 있는 기반이 된다.

---

# 9. Step 5 — TX buffer handling generalization

### 먼저 쉽게 이해하기

Step 4가 이름과 구조를 일반화한 단계였다면, Step 5에서는 **buffer 종류가 실제로 다르다는 사실을 코드가 기억하도록 만든다.**

왜 type이 필요한가?

```text
SKB
    -> dev_consume_skb_any()

XDP_TX
    -> xdp_return_frame*()

XDP_NDO
    -> DMA unmap + xdp_return_frame()
```

처럼 같은 TX completion이라도 memory를 반환하는 방법이 다르다.

그래서:

```text
ptr
+
type
```

를 함께 저장한다.

쉽게 말하면 TX ring에 packet이 들어갈 때 작은 꼬리표를 붙이는 것이다.

```text
[buffer 주소] + [이 buffer가 SKB인지 XDP인지]
```

TX completion은 그 꼬리표를 보고 올바른 cleanup을 선택한다.



`struct macb_tx_buff`를 실제로 일반화했다.

```c
enum macb_tx_buff_type {
    MACB_TYPE_SKB,
    MACB_TYPE_XDP_TX,
    MACB_TYPE_XDP_NDO,
};
```

그리고:

```text
struct sk_buff *skb
```

를:

```text
void *ptr
enum macb_tx_buff_type type
```

로 바꾸었다.

TX completion에서는 buffer type에 따라 서로 다른 release path를 사용한다.

```text
MACB_TYPE_SKB
    -> dev_consume_skb_any()

MACB_TYPE_XDP_TX
    -> xdp_return_frame()
       또는 xdp_return_frame_rx_napi()

MACB_TYPE_XDP_NDO
    -> xdp_return_frame()
```

이 abstraction이 필요한 이유는 일반 SKB와 XDP frame이 memory ownership과 반환 함수가 다르기 때문이다.

---

# 10. Step 6 — GEM native XDP

### 먼저 쉽게 이해하기

여기가 이번 포팅의 핵심 단계다.

기존에는:

```text
NIC
  ->
RX buffer
  ->
SKB
  ->
Linux network stack
```

였는데, native XDP가 들어가면:

```text
NIC
  ->
RX buffer
  ->
XDP program
  ->
verdict
```

가 먼저 실행된다.

즉 XDP가 packet을 보고:

```text
PASS
DROP
REDIRECT
TX
```

중 하나를 결정한 뒤에야 packet의 다음 행선지가 결정된다.

그래서 이 단계에서 driver가 새로 해야 하는 일은 단순히 BPF function을 호출하는 것이 아니다.

```text
XDP program이 없는 경우
    -> 기존 RX path

XDP_PASS
    -> SKB로 만들어 Linux stack

XDP_DROP
    -> buffer recycle

XDP_REDIRECT
    -> CPUMAP / XSKMAP 등으로 전달

XDP_TX
    -> TX ring으로 재사용
```

처럼 **verdict마다 packet memory의 다음 owner와 다음 execution path를 올바르게 처리하는 것**이다.

그래서 `xdp_rxq_info`, `xdp_buff`, page pool, RCU BPF program pointer, TX buffer abstraction이 한꺼번에 연결된다.



이 단계에서 실제 native XDP execution point가 macb GEM RX path에 추가된다.

`macb.h`에는:

```text
#include <net/xdp.h>

struct macb_queue
    struct xdp_rxq_info xdp_rxq

struct macb
    struct bpf_prog __rcu *prog
```

가 추가되었다.

핵심 함수:

```text
gem_xdp_run()
gem_xdp_setup()
gem_xdp()
gem_xdp_valid_mtu()
macb_xdp_submit_frame()
macb_tx_release_buff()
```

## 10.1 XDP RXQ와 Page Pool 연결

Page Pool이 단순 memory allocator로 끝나는 것이 아니라 XDP memory model에 연결된다.

```text
page_pool_create()
    |
    +-- xdp_rxq_info_reg()
    |
    +-- xdp_rxq_info_reg_mem_model()
           |
           +-- MEM_TYPE_PAGE_POOL
```

XDP가 붙어 있으면 DMA direction도 `DMA_BIDIRECTIONAL`, XDP가 없으면 일반 RX에 맞춰 `DMA_FROM_DEVICE`를 사용하도록 구성한다.

## 10.2 XDP program 설치

Netdev operation:

```text
.ndo_bpf = gem_xdp
```

를 추가했다.

`gem_xdp()`는 `XDP_SETUP_PROG` 명령을 받아 `gem_xdp_setup()`으로 넘긴다.

`gem_xdp_setup()`에서는 실행 중인 interface의 RX ring과 page pool을 필요에 따라 닫고 다시 열어, 새 XDP program 상태를 반영한 RX layout을 재구성한다.

개념:

```text
old RX ring/page pool
        |
     close
        |
prog pointer replace (RCU)
        |
     open
        |
new RX ring/page pool
```

이 과정에서 XDP headroom과 DMA direction도 새 상태에 맞춰 바뀐다.

## 10.3 gem_xdp_run

`gem_xdp_run()`은 RX buffer를 `xdp_buff`로 준비하고 실제 BPF program을 실행한다.

```text
rcu_read_lock()
    ->
rcu_dereference(bp->prog)
    ->
xdp_init_buff()
    ->
xdp_prepare_buff()
    ->
bpf_prog_run_xdp()
    ->
verdict
```

verdict 처리:

```text
XDP_PASS
    -> SKB 생성
    -> napi_gro_receive()

XDP_DROP
    -> page_pool 반환

XDP_REDIRECT
    -> xdp_do_redirect()
    -> xdp_do_flush()

XDP_TX
    -> xdp_convert_buff_to_frame()
    -> macb_xdp_submit_frame()

XDP_ABORTED
    -> buffer 반환 + exception trace
```

## 10.4 XDP_PASS

단일 descriptor frame에서는:

```text
RX buffer
    ->
gem_xdp_run()
    ->
XDP_PASS
    ->
napi_build_skb()
    ->
skb_reserve()
    ->
skb_mark_for_recycle()
    ->
skb_put()
    ->
napi_gro_receive()
```

으로 기존 Linux network stack에 연결된다.

## 10.5 XDP_TX

XDP_TX는 기존 RX buffer를 재사용할 수 있도록 DMA synchronization을 적용하고 macb TX ring에 frame을 제출한다.

```text
RX buffer
    ->
XDP_TX
    ->
DMA sync for device
    ->
TX descriptor
    ->
hardware
```

## 10.6 MTU 제한

XDP buffer에 필요한:

```text
IP alignment
+
MTU
+
XDP_PACKET_HEADROOM
+
skb_shared_info
```

가 하나의 page 안에 들어갈 수 있는지를 확인한다.

핵심 검사는 개념적으로:

```c
__gem_total_rx_buffer_size(..., XDP_PACKET_HEADROOM)
    <= PAGE_SIZE
```

이다.

따라서 XDP가 활성화된 상태에서는 지원할 수 없는 MTU를 driver가 거부할 수 있다.

---

# 11. Step 7 — ndo_xdp_xmit

### 먼저 쉽게 이해하기

Step 6의 XDP_TX는 "내가 RX로 받은 frame을 다시 내 NIC로 보내는 것"에 가깝다.

`ndo_xdp_xmit`은 조금 다르다.

```text
다른 interface / 다른 XDP path
            |
            v
       xdp_frame
            |
            v
       macb driver
            |
            v
          TX ring
```

처럼 **driver 외부에서 넘어온 XDP frame을 macb가 받아서 송신할 수 있게 하는 진입점**이다.

그래서 Step 5에서 준비한 `MACB_TYPE_XDP_NDO`가 여기서 실제 의미를 갖는다.



마지막 kernel patch는 `.ndo_xdp_xmit`을 추가한다.

```text
.ndo_xdp_xmit = gem_xdp_xmit
```

주요 함수:

```text
gem_xdp_xmit()
macb_xdp_submit_frame()
macb_tx_unmap()
macb_tx_release_buff()
```

`gem_xdp_xmit()`은 외부에서 전달된 `xdp_frame` 배열을 순회하면서 macb TX ring에 제출한다.

이 경우 frame이 외부 interface에서 들어온 것이므로 DMA mapping이 필요하다.

```text
MACB_TYPE_XDP_NDO
    ->
dma_map_single(... DMA_TO_DEVICE)
    ->
TX ring
    ->
completion
    ->
dma_unmap_single()
    ->
xdp_return_frame()
```

반면 RX buffer를 재사용하는 `MACB_TYPE_XDP_TX`는 별도의 DMA mapping이 아니라 적절한 synchronization path를 사용한다.

최종적으로 netdev가:

```text
NETDEV_XDP_ACT_NDO_XMIT
```

capability를 광고할 수 있게 되었다.

---


# 36. Native XDP를 userspace에서 실제로 사용하도록 변경

Kernel이 native XDP를 지원하게 된 것만으로는 기존 project application이 자동으로 native mode를 사용하는 것이 아니다.

초기 `xdp_loader.c`는:

```c
XDP_FLAGS_SKB_MODE
```

를 사용하고 있었다.

따라서 실제 attach는 Generic/SKB XDP였다.

이를:

```c
XDP_FLAGS_DRV_MODE
```

로 변경했다.

attach:

```c
bpf_xdp_attach(g_ifindex, prog_fd,
               XDP_FLAGS_DRV_MODE, NULL);
```

detach:

```c
bpf_xdp_detach(g_ifindex,
               XDP_FLAGS_DRV_MODE, NULL);
```

최종 runtime 확인:

```text
sudo bpftool net list

xdp:
eth0(2) driver id <program id>
```

가 나타났으므로 실제 driver/native XDP로 attach되었음을 확인했다.

---

# 37. BPF application의 map 구조

초기에는 CPUMAP과 XSKMAP이 동일한 128-entry constant를 공유했다.

```text
ACE_XDP_MAP_MAX_ENTRIES = 128
```

하지만 CPUMAP에는 현재 kernel의 `NR_CPUS`에 따른 별도 제한이 존재한다.

현재 target은:

```text
CONFIG_NR_CPUS=4
CPU 0, 1, 2, 3
```

이다.

따라서:

```text
CPUMAP max_entries = 128
```

이면 map creation에서:

```text
128 > NR_CPUS
```

가 되어 `-E2BIG`가 발생했다.

실제 검증에서도:

```text
max_entries=5
    -> -E2BIG

max_entries=4
    -> success
```

가 확인되었다.

반면 XSKMAP은 RX queue index를 key로 사용하는 구조라 CPU 수와 동일한 limit을 적용할 이유가 없다.

따라서 최종 configuration은:

```text
ACE_XDP_CPU_MAP_MAX_ENTRIES = 4
ACE_XDP_XSK_MAP_MAX_ENTRIES = 64
```

로 분리했다.

현재:

```text
cpu_map[3] = 256
```

이며 이 값은 CPU 3 대상 CPUMAP queue size를 의미한다.

---

# 14. AF_XDP kernel configuration

CPUMAP 문제가 해결된 다음 XSKMAP 생성에서:

```text
libbpf: map 'xsk_map': failed to create: -EINVAL
```

오류가 발생했다.

현재 kernel에 AF_XDP 관련 map type이 활성화되어 있지 않았기 때문이다.

최종 kernel configuration은:

```text
CONFIG_BPF_SYSCALL=y
CONFIG_BPF_JIT=y
CONFIG_XDP_SOCKETS=y
CONFIG_XDP_SOCKETS_DIAG=y
CONFIG_PAGE_POOL=y
CONFIG_MACB=y
CONFIG_MACB_USE_HWSTAMP=y
```

이고 mixed-criticality runtime 설정은:

```text
CONFIG_PREEMPT_RT=y
CONFIG_NO_HZ_FULL=y
CONFIG_ARM64_16K_PAGES=y
```

를 유지한다.

최종 kernel release:

```text
6.18.46-thesis-test-rt+
```

이다.

---

# 15. Kernel/modules BTF 문제

Kconfig 변경과 kernel rebuild 과정에서 동일 kernelrelease 문자열을 사용하는 서로 다른 module build 결과가 섞이면 module BTF가 현재 running kernel의 BTF와 맞지 않을 수 있다.

초기에는 다음 오류가 발생했다.

```text
failed to validate module [libsha1] BTF: -22
failed to validate module [lz4_compress] BTF: -22
```

`systemd-modules-load.service`에서도 `zram`, `i2c_dev` module loading이 실패했다.

해결책은 오류를 무시하거나 BTF 검사를 비활성화하는 것이 아니라, 동일 kernel source/configuration에서 kernel image와 module tree를 같이 다시 build하여 배포하는 것이었다.

최종 runtime에서:

```text
systemd-modules-load.service
    active (exited)
    status=0/SUCCESS
```

이고:

```text
dmesg | grep -Ei 'BTF|failed to validate module'
```

에서 module BTF validation 오류가 없음을 확인했다.

재현 시 중요한 규칙:

```text
vmlinux/Image
+
/lib/modules/<kernelrelease>
```

는 반드시 동일 build state에서 생성된 세트를 사용한다.

---

# 16. Cross compilation

Raspberry Pi에서 kernel 전체 module tree를 native build하는 것은 반복 실험에서 비효율적이었다.

따라서 x86_64 VMware Ubuntu build host로 전환하고 ARM64 cross compiler를 사용한다.

```text
build host:
x86_64
Ubuntu 25.04

target:
ARM64 Raspberry Pi 5

compiler:
aarch64-linux-gnu-gcc
```

예시:

```bash
make ARCH=arm64 \
     CROSS_COMPILE=aarch64-linux-gnu- \
     -j16 Image.gz modules
```

module staging:

```bash
make ARCH=arm64 \
     CROSS_COMPILE=aarch64-linux-gnu- \
     INSTALL_MOD_PATH=~/module-staging \
     modules_install
```

이후 동일 release의 module tree와 kernel image를 Raspberry Pi에 함께 배포한다.

---

# 17. 실제 native XDP 기본 테스트

## 17.1 XDP_PASS

map이 없는 최소 BPF program:

```c
SEC("xdp")
int xdp_pass_prog(struct xdp_md *ctx)
{
    return XDP_PASS;
}
```

native attach:

```bash
sudo ip link set dev eth0 xdpdrv \
    obj ./xdp_pass.o sec xdp
```

검증 결과:

```text
bpftool net list
    eth0(2) driver id <id>
```

그리고 실제 Sender Pi에서 Ethernet traffic을 주입한 결과, `tcpdump`에서 실제 packet이 관찰되어:

```text
Sender
    ->
eth0 RX
    ->
macb native XDP
    ->
XDP_PASS
    ->
Linux network stack
```

경로를 확인했다.

## 17.2 XDP_DROP

최소 DROP program:

```c
SEC("xdp")
int xdp_drop_prog(struct xdp_md *ctx)
{
    return XDP_DROP;
}
```

native attach 후 `bpftool prog run`에서:

```text
Return value: 1
```

을 확인했다.

또한 driver가 정상적으로 detach/reinitialize되었고 kernel panic이나 관련 kernel error가 발생하지 않았다.

---

# 18. 실제 CPUMAP 검증

DUT에서 project loader를 native mode로 실행하면:

```text
xdp_dispatch
    +
cpu_map[3] = 256
    +
xsk_map
    +
stats
```

가 준비된다.

9001 packet:

```text
Sender
    |
    | UDP dst 9001
    v
DUT eth0
    |
    v
macb RX
    |
    v
native XDP
    |
    v
bpf_redirect_map(cpu_map, 3, XDP_PASS)
    |
    v
CPUMAP queue
    |
    v
CPU 3 cpumap kthread
    |
    v
Linux network stack
```

실제 packet test에서 9001 packet 1000개를 넣었고 CPUMAP 통계가 증가했다.

여기서 중요한 해석 규칙은 stats가 `BPF_MAP_TYPE_PERCPU_ARRAY`라는 것이다.

```text
stats[key][cpu]
```

이므로:

```text
stats[CPUMAP][CPU1] = 1000
```

은 "packet 1000개가 CPU 1로 redirect됐다"는 뜻이 아니다.

그 의미는:

> CPUMAP 통계 counter를 증가시키는 BPF 코드가 CPU 1에서 1000번 실행되었다.

이다.

실제 CPUMAP destination CPU는 별도의:

```text
cpu_map[3]
```

으로 결정된다.

이 차이를 분리해서 해석해야 한다.

## 18.1 CPU 3에서 실제 CPUMAP worker 실행

tracefs에서 다음 event가 확인되었다.

```text
xdp:xdp_cpumap_enqueue
xdp:xdp_cpumap_kthread
```

실제 trace 예:

```text
cpumap/3/map:<id> ... [003] ...
xdp_cpumap_kthread:
    kthreadcpu=3
    map_id=<id>
    action=REDIRECT
    processed=1
```

또한:

```bash
ps -eLo pid,tid,psr,comm | grep -i cpumap
```

에서:

```text
cpumap/3/map:<id>
PSR = 3
```

인 kthread가 확인되었다.

따라서 CPUMAP은 단순 map creation 또는 BPF counter 증가가 아니라 **실제 CPU 3의 CPUMAP worker 실행까지 확인된 상태**다.

---

# 19. 실제 AF_XDP 검증

AF_XDP의 runtime path는:

```text
Sender
    |
    | UDP dst 9002
    v
DUT eth0
    |
    v
native XDP
    |
    v
xsk_map[RX queue]
    |
    v
AF_XDP socket
    |
    v
UMEM
    |
    v
RX ring
    |
    v
afxdp_recv userspace
```

이다.

기본 queue는:

```text
0
```

이다.

runtime 순서:

```bash
# DUT terminal 1
sudo ./xdp_loader eth0

# DUT terminal 2
sudo ./afxdp_recv eth0 0
```

이때 loader는 먼저:

```text
BPF object load
CPUMAP setup
native XDP attach
map pin
```

을 완료해야 한다.

그 후 `afxdp_recv`가 pinned `xsk_map`을 열고 queue 0에 AF_XDP socket을 등록한다.

Sender에서 UDP 9002 packet 100개를 전송했으며 `afxdp_recv`에서 100개를 실제 수신했다.

XSK stats도 100개에 대응하여 증가했다.

즉 단순 BPF program execution이 아니라:

```text
wire packet
    ->
native XDP
    ->
XSKMAP
    ->
AF_XDP
    ->
userspace receive
```

까지 실제 검증했다.

---

# 20. Mixed traffic

마지막으로 세 execution path를 한 번에 섞는 테스트를 수행했다.

```text
PASS       300
CPUMAP     200
AF_XDP     100
총         600
```

테스트용 일반 port는 classifier에서 "other"로 인식되도록 사용한다. 예를 들어 9003을 사용할 수 있다.

```text
UDP 9003
    -> XDP_PASS

UDP 9001
    -> CPUMAP[3]

UDP 9002
    -> XSKMAP[0]
```

실행 순서는 중요하다.

```text
1. xdp_loader 실행
2. xdp_loader가 map pin 완료
3. afxdp_recv 실행
4. XSKMAP[0] socket 등록
5. Sender에서 mixed traffic 전송
6. stats 확인
7. afxdp 수신 count 확인
```

loader가 종료되면 현재 구현에서는 XDP detach와 map unpin이 수행되므로 AF_XDP receiver의 pinned map dependency도 사라진다. 따라서 loader와 receiver는 독립적인 lifecycle로 취급하지 않는다.

이번 mixed test에서는 CPUMAP 200개와 AF_XDP 100개가 분리되어 처리되는 것을 확인했다. PASS traffic은 일반 stack으로 전달되는 path에 해당한다.

---

# 21. 현재 구현과 검증 결과

현재 기능 수준은 다음과 같이 정리할 수 있다.

```text
Raspberry Pi 5 kernel boot
    PASS

PREEMPT_RT
    PASS

16 KB pages
    PASS

NO_HZ_FULL
    PASS

macb normal RX regression
    PASS

page_pool RX
    PASS

BPF object load
    PASS

CPUMAP creation
    PASS

XSKMAP creation
    PASS

native XDP_PASS attach
    PASS

native XDP_PASS real wire path
    PASS

native XDP_DROP
    PASS

CPUMAP redirect
    PASS

CPUMAP worker on CPU 3
    PASS

AF_XDP userspace receive
    PASS

mixed traffic
    PASS
```

단, 이것은 기능적 검증이다.

아직 다음을 측정하지 않은 상태라면 deterministic performance 결론을 내려서는 안 된다.

```text
latency distribution
tail latency
jitter
packet loss under load
deadline miss ratio
CPU contention sensitivity
network-rate sensitivity
```

---

# 22. 향후 measurement architecture

최종 연구에서는 packet마다 적어도:

```c
struct capture_record {
    __u64 sequence;
    __u64 hw_rx_timestamp_ns;
    __u64 user_rx_timestamp_ns;
    __u32 packet_length;
    __u16 path;
    __u16 flags;
};
```

형태의 정보를 수집하는 것을 목표로 한다.

기본 latency:

```text
latency = user_rx_timestamp - hw_rx_timestamp
```

단 HW timestamp가 PHC clock domain이고 userspace timestamp가 `CLOCK_MONOTONIC_RAW`라면 두 시간을 직접 뺄 수 없다.

따라서:

```text
PHC
  |
  +-- calibration
  |
CLOCK_MONOTONIC_RAW
```

사이의 관계를 먼저 보정해야 한다.

측정 과정에서 packet마다 `printf()`를 수행하면 I/O와 scheduling overhead가 측정에 영향을 줄 수 있으므로 가능한 구조는:

```text
preallocated buffer
    ->
fixed-size record
    ->
batch write
    ->
offline analysis
```

이다.

주요 통계:

```text
mean
p50
p95
p99
p99.9
max
jitter
packet loss
deadline miss ratio
```

특히 mixed-criticality 연구에서는 평균 latency보다 tail behavior가 중요하다.

---

# 23. Load sweep

초기 CPU contention 조건은 예를 들어:

```text
0%
20%
40%
60%
80%
100%
```

으로 증가시킬 수 있다.

network packet rate도 독립 변수로 두고:

```text
CPU load
x
packet rate
```

형태의 실험 matrix를 구성할 수 있다.

처음부터 CPU load, packet rate, memory pressure를 모두 동시에 바꾸지 않는다.

최소 비교군:

```text
Baseline
    normal Linux UDP/socket path

RT baseline
    PREEMPT_RT + CPU placement/affinity + kernel path

Proposed kernel path
    XDP -> CPUMAP -> CPU 3 -> kernel stack

Proposed userspace path
    XDP -> AF_XDP -> userspace
```

연구의 질문은 특정 path가 모든 상황에서 가장 빠른가가 아니다.

목표는 mixed-criticality traffic을 요구사항에 따라 분리했을 때 각 execution path의 latency와 tail behavior를 더 예측 가능한 형태로 만들 수 있는지를 확인하는 것이다.

---

# 24. 중요한 runtime 설계 구분

## CPUMAP vs CPU affinity

CPUMAP은 packet-level steering이다.

```text
packet
    ->
CPUMAP[3]
    ->
CPU 3
```

반면 IRQ affinity, RPS, CPU isolation은 network work가 어떤 execution environment에서 실행되는지 제어한다.

따라서:

```text
CPUMAP[3]
```

을 설정한 것만으로 CPU 3 전체가 RT-isolated 상태가 되는 것은 아니다.

최종 RT measurement에서는 최소한:

```text
NIC IRQ placement
RPS
CPU affinity/isolation
CPUMAP destination
```

를 서로 다른 configuration으로 취급해야 한다.

## Per-CPU stats vs steering destination

stats:

```text
stats[key][cpu]
```

CPUMAP:

```text
cpu_map[cpu]
```

는 완전히 다른 의미다.

```text
stats[1][1] = 1000
```

은 counter execution CPU를 의미하고,

```text
cpu_map[3] = 256
```

은 CPUMAP destination CPU 3과 queue size 256을 의미한다.

---

# 25. 재현 절차 요약

## Kernel

VMware build host에서:

```bash
cd ~/linux-6.18-rt

make ARCH=arm64 \
     CROSS_COMPILE=aarch64-linux-gnu- \
     -j16 Image.gz modules

make ARCH=arm64 \
     CROSS_COMPILE=aarch64-linux-gnu- \
     INSTALL_MOD_PATH=~/module-staging \
     modules_install
```

target에 kernel image와 해당 release의 module tree를 함께 배포한다.

부팅 후:

```bash
uname -a
uname -v
systemctl status systemd-modules-load.service --no-pager -l
dmesg | grep -Ei 'BTF|failed to validate module'
```

확인.

## BPF

```bash
cd ~/ACEteam_Bachelor_thesis/BPF
make
```

native loader:

```bash
sudo ./xdp_loader eth0
```

AF_XDP:

```bash
sudo ./afxdp_recv eth0 0
```

기본 classifier:

```text
9001 -> CPUMAP CPU 3
9002 -> AF_XDP
other -> PASS
```

테스트 종료:

```text
Ctrl-C
```

로 loader를 정상 종료하여 detach/unpin하도록 한다.

---

# 26. Kernel patch export

kernel 전체 source를 application repository에 복사하지 않는다.

최종 branch에서:

```bash
cd ~/rt-experiment/linux-6.18-rt

git format-patch \
    rpi-6.18.y..thesis-macb-xdp \
    -o /tmp/macb-patches
```

로 patch series를 만든다.

필요하면 PROJECT_ROOT에:

```text
kernel-patches/
```

를 만들고 최종 patch만 저장한다.

재현을 위해서는:

```text
matching Raspberry Pi Linux source
+
PREEMPT_RT configuration
+
kernel patch series
+
BPF/application repository
```

를 함께 보존해야 한다.

---

# 27. 결론

이번 작업의 핵심은 macb driver에 단순히 BPF program call을 추가한 것이 아니다.

기존:

```text
SKB-centered RX lifecycle
```

을:

```text
page_pool
    ->
XDP-compatible RXQ
    ->
xdp_buff
    ->
native XDP
    ->
verdict-dependent buffer ownership
```

구조로 전환했다.

그 과정에서:

```text
RX buffer lifecycle
    ->
rx_buff abstraction
    ->
page_pool
    ->
multi-descriptor RX
    ->
TX buffer generalization
    ->
XDP execution
    ->
ndo_xdp_xmit
```

의 dependency 순서를 지켰다.

그 위에 BPF application이:

```text
9001 -> CPUMAP -> CPU 3
9002 -> XSKMAP -> AF_XDP
other -> XDP_PASS
```

의 packet-level classifier로 동작한다.

실제 runtime에서 native XDP attach를 `driver` mode로 확인했고, 실제 Ethernet traffic으로 XDP_PASS 경로를 검증했으며, CPUMAP은 tracepoint와 CPU 3 kthread를 통해 실제 target execution을 확인했다. AF_XDP는 실제 100 packet을 userspace receiver에서 수신했다.

따라서 현재 상태는:

```text
driver implementation
        +
BPF datapath
        +
native attach
        +
CPUMAP
        +
AF_XDP
        +
wire-level functional verification
```

이 완료된 상태다.

이후 핵심 연구 작업은 구현 자체보다 measurement infrastructure를 구축하고, HW RX timestamp와 userspace timestamp를 동일한 clock domain으로 보정한 뒤, CPU/network load를 변화시키면서 각 execution path의 latency distribution과 tail behavior를 정량적으로 비교하는 것이다.
