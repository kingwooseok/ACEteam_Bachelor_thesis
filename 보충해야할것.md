# `구현해야할것.md` 보충 및 수정 사항

> 점검 기준일: 2026-09-10
>
> 이 문서는 `드라이버 포팅한 그 모든것을 담아.md`, `구현해야할것.md`, 현재 BPF/사용자 프로그램, macb 드라이버 소스와 실장치 런타임 상태를 함께 점검한 결과다.
>
> 기존 계획을 폐기하는 문서가 아니라, 확인한 정정·누락 사항과 현재 반영 상태,
> 실장치에서 남은 검증을 구분해 정리한 보충 문서다.

## 1. 결론

현재 계획의 큰 방향은 맞다.

- macb RX descriptor의 하드웨어 timestamp를 XDP에서 읽을 수 있게 한다.
- PHC 시간과 `CLOCK_MONOTONIC`을 대응시킨다.
- XDP, CPUMAP, AF_XDP, 일반 UDP socket 경로의 지연을 비교한다.
- 같은 packet ID를 기준으로 커널과 사용자 공간 기록을 결합한다.

초기 점검에서 다음 문제가 확인됐고, 외부 Ethernet 없이 수정 가능한 source
항목은 현재 workspace에 반영했다.

1. 현재 드라이버 설정에서는 `bpf_xdp_adjust_meta()`가 동작하지 않는다.
2. RP1에서 XDP TX doorbell 처리가 일반 SKB TX 경로와 다르다.
3. 다중 RX descriptor 프레임이 Native XDP를 우회한다.
4. XDP attach/detach가 인터페이스 close/open과 PTP clock 재등록을 유발한다.
5. 혼합 실험에서 서로 다른 경로의 sequence가 충돌한다.
6. AF_XDP receiver가 장시간 실행되면 UMEM frame address를 유실할 수 있다.
7. baseline, clock domain, 실제 전달 결과의 정의가 아직 충분히 엄밀하지 않다.

1~3, 5~6은 코드 수정과 로컬 build까지 반영했다. 4는 loader/receiver의 HWTSTAMP
관리와 calibration 도구를 구현했지만 attach 뒤 PHC 재탐색을 포함한 실제 운용
검증이 남았다. 7은 controller/offline 분석 및 실험 설계 단계에서 계속 보완해야
한다. 아래 체크박스는 **코드 구현 완료**와 **실장치/wire 검증 완료**를 구분한다.

## 2. 현재 확인된 전체 구조

```text
RP1 GEM/macb RX DMA
  └─ page_pool 기반 RX buffer
      └─ gem_xdp_run()의 Native XDP
          ├─ UDP dst port 9001
          │    └─ CPUMAP key 3
          │         └─ CPU 3의 cpumap 처리
          │              └─ SKB 변환 → 네트워크 스택 → UDP socket
          ├─ UDP dst port 9002
          │    └─ XSKMAP[rx_queue_index]
          │         └─ AF_XDP userspace (현재 XDP_COPY)
          └─ 그 외
               └─ XDP_PASS → SKB → 네트워크 스택 → UDP socket
```

주요 구현 위치는 다음과 같다.

- BPF 분류기: `BPF/xdp_kern.c`
- BPF loader: `BPF/xdp_loader.c`
- AF_XDP receiver: `BPF/afxdp_recv.c`
- UDP sender/receiver: `udp_socket/src/`
- macb XDP RX/TX: `../rt-experiment/linux-6.18-rt/drivers/net/ethernet/cadence/macb_main.c`
- macb PTP timestamp: `../rt-experiment/linux-6.18-rt/drivers/net/ethernet/cadence/macb_ptp.c`

현재 BPF 통계 map은 21개 항목이며, 기본 경로 counter의 의미는 다음과 같다.

| index | 의미 | 주의점 |
|---:|---|---|
| 0 | PASS 선택 횟수 | 최종 UDP 수신 성공 수가 아님 |
| 1 | CPUMAP redirect 요청 횟수 | CPUMAP 소비 성공 수가 아님 |
| 2 | XSK redirect 요청 횟수 | AF_XDP 실제 수신 성공 수가 아님 |
| 3 | 전체 분류 횟수 | wire RX packet 총수와 반드시 같지는 않음 |

추가 index 4~20은 experiment PASS 요청, IPv4/header 오류, fragment PASS,
metadata/HWTS 성공·실패, ingress/CPUMAP record·duplicate·update error,
CPUMAP/XSK fallback을 구분한다. 정확한 enum은 `BPF/config.h`를 단일 기준으로
사용한다.

## 3. 현재 구현 및 런타임 상태

### 3.1 커널과 드라이버

- 부팅 커널: `6.18.46-thesis-test-rt+`
- 장치: Raspberry Pi 5 Model B Rev 1.1
- NIC: RP1 내장 Cadence GEM/macb
- PHY: Broadcom BCM54213PE
- 현재 커널 소스 branch: `thesis-macb-xdp`
- 기준 commit: `16f1da3c4e94437449d6aa151589ca0ad4b388bb`
- 점검 당시 driver HEAD: `f934dc812d686488da3cfe1cba31a4e28e60e9b8`
- 기준점 이후 macb 관련 포팅 commit: 7개
- page size: 16 KiB
- RX queue: 1개
- TX queue: 1개
- PTP clock: `/sys/class/ptp/ptp0`, 이름 `gem-ptp-timer`

부팅 커널에서는 macb XDP 함수와 AF_XDP 관련 심볼이 확인되므로 포팅된 커널이 실제로 실행 중인 것은 확인된다. 다만 이미지 안에 git commit manifest가 내장되어 있지는 않으므로, 향후에는 커널 이미지와 소스 commit을 manifest로 명시적으로 연결해야 한다.

### 3.2 XDP와 timestamp

마지막 점검 당시 부팅 kernel이 광고한 XDP 기능:

- `basic`
- `redirect`
- `ndo-xmit`

마지막 점검 당시 광고되지 않은 기능:

- XDP RX metadata feature
- XSK zero-copy feature

마지막 점검 당시의 기존 부팅 kernel 런타임 상태:

- XDP 프로그램 미부착
- `eth0` admin UP이지만 `NO-CARRIER`
- RX/TX packet counter 0
- HWTSTAMP TX: off
- HWTSTAMP RX filter: `NONE`
- IRQ 106 affinity/effective CPUs: 0-3
- `nohz_full`, `isolcpus`, `rcu_nocbs` 런타임 미적용

`CONFIG_NO_HZ_FULL=y`만으로 CPU isolation이 적용됐다고 판단하면 안 된다. 현재 실험 환경에서는 부팅 cmdline과 IRQ affinity 설정을 별도로 구성하고 기록해야 한다.

### 3.3 구현 상태를 표현할 때의 기준

| 기능 | 코드 상태 | 실장치 검증 상태 |
|---|---|---|
| page_pool RX | 구현됨 | 기존 기록상 동작 |
| Native XDP attach | 구현됨 | 기존 기록상 동작, 현재는 미부착 |
| XDP_PASS | 구현됨 | 기존 기록상 동작 |
| XDP_DROP | BPF 프로그램 있음 | `bpftool prog run` smoke test 중심, wire 검증 필요 |
| CPUMAP redirect | 구현됨 | 기존 기록상 수신 확인 |
| XSK redirect | 구현됨 | 기존 기록상 AF_XDP 수신 확인 |
| AF_XDP zero-copy | 미구현 | 해당 없음 |
| XDP_TX | RP1 공용 TX kick 반영·빌드됨 | wire/고부하 검증 필요 |
| `ndo_xdp_xmit` | RP1 공용 TX kick 반영·빌드됨 | wire/고부하 검증 필요 |
| SKB RX HW timestamp | 기존 드라이버 구현 있음 | RX filter를 켠 상태에서 재검증 필요 |
| XDP RX HW timestamp metadata | driver/BPF 구현·빌드됨 | 새 kernel boot/kfunc/wire 검증 필요 |
| multi-descriptor fail-closed DROP | driver 구현·빌드됨 | jumbo negative test 필요 |
| AF_XDP metadata/CSV 측정 | userspace 구현·빌드됨 | copy-mode wire/장시간 검증 필요 |
| PHC↔MONOTONIC calibration | 도구 구현·빌드됨 | `/dev/ptpN` 실측 필요 |
| 단일-run join/semantic 검증 | 구현·unit test됨 | 실제 wire run artifact 분석 필요 |
| 반복-run deadline/Student-t CI | 구현·unit test됨 | 독립 반복 run 수집 필요 |
| exact source/artifact manifest | 구현·unit test됨 | target run에서 capture 필요 |

문서에서는 앞으로 다음 표현을 구분한다.

- **구현됨**: 코드 경로가 존재하고 빌드됨
- **smoke test됨**: 제한된 함수/BPF 반환값 검사만 수행됨
- **wire 검증됨**: 실제 NIC 입출력과 소비 지점까지 확인됨
- **장시간 검증됨**: 충분한 packet 수와 시간 동안 stall/leak/drop 통계를 확인함

## 4. P0: 반영한 드라이버 수정과 남은 검증

### 4.1 XDP metadata headroom 활성화

초기 점검 당시 `gem_xdp_run()`은 다음과 같이 XDP buffer를 준비했다.

```c
xdp_prepare_buff(..., false);
```

위치: `macb_main.c`의 `gem_xdp_run()` 부근, 현재 약 1854행.

마지막 인자가 `false`이면 `data_meta`가 metadata 사용 가능 상태로 초기화되지
않는다. 현재 소스에서는 이를 `true`로 바꾸고 XDP_PASS 시 metadata size를
`skb_metadata_set()`에 전달했다. BPF/CPUMAP/AF_XDP 쪽 56-byte ABI와 오류
counter도 함께 구현했다.

- [x] macb XDP buffer의 기존 `XDP_PACKET_HEADROOM` 사용 확인
- [x] `xdp_prepare_buff()`를 metadata-valid 방식으로 호출
- [x] XDP 진입 시 `data_meta == data`가 되도록 초기화
- [x] `bpf_xdp_adjust_meta()` 성공/실패 통계 추가
- [x] XDP_PASS SKB에 metadata size 보존
- [x] CPUMAP remote program의 metadata bounds/ABI 검증 구현
- [x] XSK RX descriptor의 metadata 위치/ABI 검증 구현
- [ ] 새 kernel에서 PASS/CPUMAP/XSK metadata 전달 wire 검증

### 4.2 RP1 XDP TX doorbell 처리 통합

RP1에는 PCIe posted write 관련 workaround가 있다. 일반 SKB TX 경로는 다음 작업을 수행한다.

- 필요 시 `queue->tx_pending` 설정
- `TSTART` 기록
- RP1에서 NCR readback으로 posted write flush

위치: `macb_main.c`의 `macb_start_xmit()` 약 2996행과 `macb_tx_restart()` 약 2299행.

초기 `macb_xdp_submit_frame()`은 `TSTART`만 기록했다. 현재는 일반 SKB TX,
XDP_TX와 `ndo_xdp_xmit`이 `macb_tx_kick()`을 공유하며, 호출 경로별 기존 LPI wake
및 retry 의미를 보존하면서 `tx_pending`, `TSTART`, RP1 NCR readback을 적용한다.

구현 및 남은 검증:

- [x] 일반 SKB와 XDP가 공통으로 사용하는 TX kick helper 구현
- [x] `tx_pending`, `TSTART`, RP1 readback 순서를 동일하게 적용
- [ ] XDP_TX 단일 packet wire test
- [ ] `ndo_xdp_xmit` redirect wire test
- [ ] 고 PPS 및 장시간 실행에서 TX stall 여부 확인
- [ ] TX ring full, completion, wake/restart 동작 확인

이 검증이 끝나기 전에는 XDP_TX와 `ndo_xdp_xmit`을 완료로 표시하면 안 된다.

### 4.3 다중 RX descriptor가 XDP를 우회하는 문제

초기 RX 코드는 `first_frame && last_frame`인 경우에만 XDP를 실행하고, 여러
descriptor frame을 SKB 조립 경로로 보냈다. 현재 소스는 Native XDP가 활성화된
경우 해당 chain을 EOF까지 drain/recycle하고 frame 전체를 DROP한다. NAPI poll
경계를 넘어가는 chain도 queue 상태로 추적하고, 새 SOF에서 손실된 EOF 상태를
resynchronize한다.

### 확정한 정책

이 연구에서는 다음과 같이 결정한다.

- 실험 traffic은 single-descriptor packet으로 제한한다.
- XDP가 활성화된 상태에서 multi-descriptor frame이 들어오면 frame 전체를
  drain/recycle하고 DROP한다.
- XDP multi-buffer/frags는 논문 구현 범위에 포함하지 않는다.

정상 MTU 1500 실험 packet에는 영향을 주지 않으면서, 예상 밖 jumbo frame이
XDP classifier를 우회하여 일반 stack으로 전달되는 것은 막는다.

최소 요구사항:

- [x] IPv4 MTU 1500, UDP payload 최대 1472 bytes로 송신 범위 고정
- [ ] jumbo frame negative test 추가
- [x] frame당 `rx_xdp_multidesc_drops` counter 구현
- [ ] 외부 jumbo packet에서 frame DROP/counter 증가 검증
- [x] multi-descriptor를 일반 SKB stack으로 우회시키지 않는 정책 확정

기존 계획의 “어느 descriptor에서 timestamp를 읽을 것인가”만으로는 이 문제가 해결되지 않는다.

### 4.4 XDP attach/detach와 PTP lifecycle

`gem_xdp_setup()`은 XDP 프로그램 유무가 바뀔 때 netdev를 close/open한다. 위치는 `macb_main.c` 약 3870행이다. 이 과정에서 PTP clock이 unregister/register되고 TSU가 다시 초기화된다.

영향:

- `/dev/ptpN` 번호가 바뀔 수 있다.
- 이전에 열어 둔 PHC fd를 재사용할 수 없다.
- attach 전에 구한 PHC↔MONOTONIC 보정값이 무효가 될 수 있다.
- HWTSTAMP 설정도 다시 확인해야 한다.
- 실행 중인 `ptp4l`/`phc2sys`가 영향을 받을 수 있다.

실험 준비 순서를 다음으로 고정한다.

1. Native XDP 프로그램 attach
2. `/sys/class/ptp`와 netdev 연계를 다시 조회
3. `/dev/ptpN`을 다시 open
4. `SIOCSHWTSTAMP`로 RX filter 설정 또는 재확인
5. 필요하면 `ptp4l`/`phc2sys` 재시작 및 동기화
6. PHC↔`CLOCK_MONOTONIC` calibration
7. 실험 시작

detach 후 원상복구할 때도 PTP 장치를 다시 탐색하고 설정을 검증해야 한다.

현재 loader는 Native attach가 성공한 **뒤에** `HWTSTAMP_FILTER_ALL`을 SIOCS로
적용하고, detach 뒤에는 시작 시 저장한 설정을 명시적으로 다시 SIOCS로
적용한다. `phc_calibrate --interface eth0`는 `ETHTOOL_GET_TS_INFO`로 attach 뒤
현재 PHC index를 자동 탐색한다.

- [x] attach 뒤 HWTSTAMP filter 재적용 코드
- [x] detach 뒤 기존 HWTSTAMP 설정 명시적 복구 코드
- [x] interface 기반 PHC device 자동 탐색
- [ ] 새 kernel에서 attach/detach 전후 PHC index/filter 실제 확인

## 5. Timestamp 구현 방향 보완

### 5.1 기존 descriptor timestamp 코드를 공용 helper로 분리

현재 `macb_ptp.c`에는 이미 다음 로직이 있다.

- descriptor timestamp valid bit 검사
- 32비트/64비트 descriptor layout 처리
- descriptor의 초 하위 비트와 현재 TSU 시간을 이용한 전체 초 복원
- nanosecond 값 생성

주요 함수:

- `macb_ptp_desc()`
- `gem_hw_timestamp()`
- `gem_ptp_rxstamp()`

현재 `gem_ptp_rxstamp_ns()`를 공용 helper로 분리해 기존 SKB 경로와 XDP metadata
callback이 같은 descriptor decode를 사용한다. HW PTP/filter/RXVALID와 descriptor
layout을 확인하고 ns 또는 명확한 오류를 반환한다.

권장 구조:

```text
descriptor → 공용 timestamp decode helper
                 ├─ 기존 SKB RX timestamp 경로
                 └─ XDP RX metadata callback
```

구현 및 남은 검증:

- [x] descriptor format별 공용 decode helper 작성
- [x] timestamp valid가 아니면 명확한 오류 반환
- [ ] 기존 SKB timestamp 결과가 바뀌지 않는지 회귀 테스트
- [x] XDP wrapper에서 현재 RX descriptor에 접근
- [x] `xdp_metadata_ops`의 RX timestamp callback 연결
- [x] device-bound BPF load 적용
- [ ] 새 kernel에서 metadata capability/kfunc load와 값 검증

### 5.2 wrapper lifetime과 descriptor 소유권

wrapper의 첫 필드로 `struct xdp_buff`를 두고, 그 뒤에 `bp`, descriptor pointer 등 macb 전용 정보를 두는 방향은 타당하다.

다만 다음을 명시해야 한다.

- callback이 호출될 때 descriptor가 아직 DMA/RX ring에 유효해야 한다.
- page를 recycle하거나 descriptor ownership을 돌려준 뒤 callback을 호출하면 안 된다.
- multi-descriptor frame은 Native XDP 활성 중 DROP하므로 callback 대상이 아니다.
- BPF helper 반환값이 실패한 packet도 별도 집계해야 한다.

### 5.3 현재 RX filter가 `NONE`임

마지막 런타임 점검에서 장치의 HWTSTAMP RX filter는 `NONE`이었다. Loader와 UDP
receiver에 기존 설정 저장, `HWTSTAMP_FILTER_ALL` 요청/effective 값 확인, 안전한
복구 코드를 구현했지만 실제 NIC에서는 다시 확인해야 한다.

Phase 1 baseline에서 반드시 해야 할 것:

- [x] 기존 HWTSTAMP 설정 저장 코드
- [x] `HWTSTAMP_FILTER_ALL` 요청 코드
- [x] 드라이버가 반환한 effective filter 확인/출력 코드
- [ ] 일반 UDP `recvmsg()` ancillary timestamp 확인
- [x] XDP metadata helper 성공/실패 counter 구현
- [x] 실험 종료 시 외부 변경을 덮어쓰지 않는 기존 설정 복구 코드
- [x] XDP attach 뒤 filter 설정과 interface 기반 PHC 재탐색 도구
- [ ] 새 kernel에서 PHC index/effective filter 실측 검증

## 6. Clock domain 및 보정 방법 수정

### 6.1 주 시간축

BPF의 `bpf_ktime_get_ns()`는 `CLOCK_MONOTONIC` 계열과 대응시켜야 한다. 따라서 주 보정 방법은 다음과 같이 잡는다.

- PHC: NIC hardware timestamp
- system clock: `CLOCK_MONOTONIC`
- 보정: `PTP_SYS_OFFSET_EXTENDED`에서 `clockid = CLOCK_MONOTONIC`

`PTP_SYS_OFFSET_PRECISE`는 주 방법의 단순 fallback으로 취급하면 안 된다.

- macb의 `ptp_clock_info`에는 `getcrosststamp`가 없어 PRECISE ioctl을 지원하지 않는다.
- 지원되더라도 PRECISE가 주는 system timestamp domain은 MONOTONIC과 동일하지 않다.
- `ethtool -T`의 provider qualifier인 “Precise”와 `PTP_SYS_OFFSET_PRECISE` 지원 여부는 서로 다른 개념이다.

`udp_socket/src/phc_calibrate.c`에 이 방식을 구현했다. 각
`[CLOCK_MONOTONIC-before, PHC, CLOCK_MONOTONIC-after]` 표본과 system span,
`PHC-MONOTONIC` offset을 CSV로 저장하고 최소-span 표본을 표시한다. Build와
`--help` 검사는 완료했지만 현재 계정은 `/dev/ptp0` 접근 권한이 없어 실제 ioctl
측정은 아직 수행하지 못했다.

### 6.2 보정값 품질 기록

각 calibration에 다음을 기록한다.

- PHC device/index
- calibration 전후 system timestamp
- 선택된 최소 round-trip sample
- offset
- sample spread/jitter
- calibration 시각
- XDP attach/detach generation

PHC↔MONOTONIC 변환 오차를 latency 결과와 분리하여 함께 보고해야 한다.

## 7. Packet ID와 map 구조 수정

### 7.1 해결한 sequence 충돌

Sender별 sequence가 0부터 시작하는 것은 유지하되, 혼합 실험의 9001/9002/PASS
sender에 서로 다른 `flow_id`를 준다. BPF ARRAY 대신 아래 16-byte 복합 key의 일반
HASH를 구현해 같은 sequence의 record 충돌을 해결했다.

### 확정한 packet 식별 및 저장 정책

- 한 run 안의 packet ID는 `flow_id + sequence`로 한다.
- `run_id`는 wire packet이나 BPF map key에 넣지 않고 결과 디렉터리와 run
  manifest에서 관리한다.
- run 시작 전에 measurement map을 비운다.
- BPF 기록은 일반 `BPF_MAP_TYPE_HASH`에 저장한다.
- `LRU_HASH`는 자동 축출이 실제 packet loss와 혼동되므로 사용하지 않는다.

확정한 map key:

```c
struct ace_packet_key {
    __u32 flow_id;
    __u32 reserved;
    __u64 sequence;
};
```

각 sender/traffic stream에는 고유한 flow ID를 부여한다. 같은 sequence를 가진
9001/9002/PASS traffic도 flow ID가 다르므로 record가 충돌하지 않는다. UDP
sender와 receiver 모두 `--flow-id`를 필수로 받으며, 한 loader run의 동시
flow/path에는 서로 다른 값을 배정해야 한다.

Wire ABI는 `include/ace_packet_abi.h`의 32-byte
`struct ace_experiment_header`를 BPF와 UDP가 공유하며, 기본 BPF/AF_XDP record
capacity는 262,144개다.

### 7.2 unaligned payload 처리

Ethernet + IPv4 + UDP 뒤 payload는 흔히 byte offset 42에서 시작한다. 이 주소를 `u64 *`로 cast해 직접 역참조하면 정렬 문제가 생길 수 있다.

구현 상태:

- [x] bounded copy 뒤 64비트 값 decode
- [x] Ethernet/IP/UDP 길이와 `data_end` 모두 검증
- [x] IPv4 version/IHL 검증
- [x] `tot_len`과 UDP length 검증
- [x] fragment 여부 검증
- [x] wire byte order를 공용 ABI header에 고정

### 7.3 요청 경로와 실제 전달 결과 분리

BPF가 기록한 path는 실제 수신 성공 경로가 아니라 classifier의 결정이다.

예:

- `bpf_redirect_map()`을 호출했지만 redirect가 실패할 수 있다.
- CPUMAP enqueue 뒤 consumer까지 전달되지 않을 수 있다.
- XSK ring이 full이면 AF_XDP가 packet을 받지 못할 수 있다.

따라서 다음을 분리한다.

- `requested_path`: XDP classifier가 선택한 동작
- `redirect_result`: redirect helper 및 XDP action 결과
- `observed_sink`: UDP, CPUMAP, AF_XDP 중 실제로 기록을 남긴 consumer
- `drop_reason`: ring full, invalid descriptor, no map entry 등

## 8. 지연 구간의 이름과 해석 수정

기존 계획의 timestamp 차이는 순수한 단일 컴포넌트 시간이 아니다.

| 계산값 | 실제 포함 구간 |
|---|---|
| `t_xdp - t_hw` | NIC timestamp 지점 이후 DMA/IRQ/NAPI/XDP 진입 및 clock 변환 오차 |
| `t_cpumap - t_xdp` | initial BPF 나머지 실행, map 기록, redirect/enqueue, CPUMAP scheduling까지 포함 |
| `t_user - t_cpumap` | remote BPF 나머지, SKB 생성, 네트워크 스택, socket wakeup까지 포함 |
| `t_user - t_hw` | 전체 RX-to-userspace 지연과 clock 보정 오차 포함 |

따라서 `t_cpumap - t_xdp`를 “순수 CPUMAP queue latency”라고 부르지 않는다. 필요하면 redirect 직전 timestamp를 추가하되, instrumentation overhead도 같이 측정해야 한다.

`ace_rx_meta.cpumap_ns`와 별도 `cpumap_records` map에 같은 값을 중복 저장할 계획이라면 목적을 정리한다. 일반 UDP 사용자에게 XDP metadata가 그대로 전달되지 않으므로 오프라인 결합에는 별도 map이 더 적합하다.

### 8.1 구현한 offline analyzer

`analysis/join_results.py`는 필수 `--manifest`/ingress/하나 이상의 PHC
calibration과 선택적 CPUMAP/AF_XDP/UDP record를 `flow_id + sequence`로 full
outer join한다. Manifest version/`run_id`와 모든 입출력의 resolved run
directory 경계를 검증한다. Requested path, metadata/validation flag,
timestamp/error 조합과 ingress-CPUMAP/AF_XDP/UDP sink 일치도 검사해,
모순된 raw row는 보존하되 관련 metric에서 제외한다.

PHC calibration의 selected 표본은 시간순으로 선형 보간한다. 범위 밖
timestamp는 기본 clock-crossing metric에서 제외하고 out-of-range로 집계한다.
명시적 `--allow-endpoint-clamp`는 endpoint offset을 사용하는 탐색적
분석용이며, 최종 run은 직전/직후 calibration이 측정 구간을 bracket해야
한다. CSV/summary를 `O_EXCL`/`fsync()`한 뒤 모든 입력과 두 출력의
크기/SHA-256을 묶은
completion marker를 마지막에 만든다.

- [x] source별 presence와 requested path를 보존하는 full outer join
- [x] `xdp-hw`, `cpumap-xdp`, `user-cpumap`, `user-xdp`, `user-hw` 계산
- [x] path/flag/timestamp/sink semantic 검증과 inconsistency 집계
- [x] duplicate/conflict/missing/negative와 summary 통계 생성
- [x] calibration out-of-range 기본 제외 및 명시적 exploratory clamp 정책
- [x] manifest/run 경계 검증, joined/summary/completion `O_EXCL`/`fsync()` 저장
- [x] interpolation, metric, semantic rejection, completion/overwrite unit test
- [x] 여러 독립 run의 aggregation·deadline·양측 95% Student-t CI 분석
- [ ] class별 deadline orchestration과 instrumentation-on/off 비교 자동화

`analysis/aggregate_runs.py`는 최소 두 개의 join completion marker만 입력으로
받고 CSV/summary digest, manifest/run ID/directory, manifest parameter,
interface/kernel/source/artifact cohort와 분석 정책을 재검증한다. Pooled packet
p50/p95/p99/관측 최댓값/deadline miss와, 각 run의 p50/p95/p99·miss rate를
동일 가중한 평균의 양측 95% Student-t CI를 분리해 출력한다. 독립
단위가 아닌 pooled packet 통계에는 CI를 붙이지 않는다.

## 9. Baseline 및 실험군 수정

### 9.1 일반 UDP baseline에 HW timestamp를 필수화

C0-C2의 primary metric이 hardware RX-to-user latency라면, 일반 UDP receiver에서도 다음을 반드시 수집해야 한다.

- `recvmsg()` 수신 시점의 `CLOCK_MONOTONIC`
- `SO_TIMESTAMPING` ancillary data의 raw hardware RX timestamp
- packet ID/sequence
- CPU ID
- 오류 및 누락 상태

이를 선택적 validation으로 두면 XDP 경로와 공정한 비교가 불가능하다.

### 9.2 C0와 C1의 confound 제거

vanilla kernel과 RT kernel의 드라이버 patch set까지 다르면 다음 효과가 섞인다.

- PREEMPT_RT 효과
- page_pool 전환 효과
- RX allocation/lifecycle 변화
- XDP 포팅 과정의 드라이버 변화

주 비교군은 동일한 kernel base와 동일한 macb patch set을 사용하고 preemption 설정만 바꿔야 한다. stock driver와 patched driver 비교가 필요하면 별도 실험축으로 둔다.

### 9.3 혼합 트래픽 control 추가

C6 혼합 실험과 같은 aggregate PPS, packet size, class ratio를 사용하면서 모든 packet을 XDP_PASS로 처리하는 control을 추가한다. 그래야 경로 분리 자체가 latency와 deadline miss에 주는 효과를 분리할 수 있다.

### 9.4 deadline 정의

각 class/path에 대해 다음을 사전에 명시한다.

- deadline 값
- 값의 선정 근거
- 어느 timestamp부터 어느 timestamp까지 적용되는지
- PHC 보정 오차를 deadline 판정에 어떻게 반영하는지
- warm-up과 측정 구간
- observed maximum과 percentile

유한한 실행에서 얻은 최댓값은 “worst-case latency”가 아니라 “observed maximum latency”라고 표현한다.

## 10. BPF 프로그램 보완 사항

### 10.1 IPv4 fragment 처리

현재 parser는 IPv4 version/IHL/`tot_len`, UDP length와 `data_end`를 함께
검증한다. MF 또는 non-zero fragment offset이 있는 packet은 L4 header로
해석하지 않고 counter를 증가시킨 뒤 XDP_PASS한다. VLAN은 이번 실험 범위에서
지원하지 않는 것으로 문서화했다.

- [x] IPv4 version 확인
- [x] IHL 최소/최대 및 `data_end` 확인
- [x] fragment offset/MF 검사
- [x] fragment packet 정책을 PASS로 명시
- [x] VLAN 미지원 범위를 문서에 명시
- [ ] malformed/fragment/VLAN wire negative test

### 10.2 redirect counter 의미 수정

CPUMAP/XSK counter는 전달 성공이 아니라 redirect 요청 또는 분류 횟수다. 이름을 이에 맞게 바꾸거나 문서에서 의미를 정확히 정의한다.

실제 전달 성공은 다음 기록과 결합한다.

- CPUMAP remote program counter/record
- AF_XDP RX count
- UDP receiver record
- redirect error 및 exception tracepoint
- netdev/XDP drop statistics

### 10.3 loader ownership과 detach 안전성

Loader와 AF_XDP receiver에 singleton owner 정책과 ID/map schema 검증을 구현했다.

- [x] `XDP_FLAGS_UPDATE_IF_NOEXIST` 사용
- [x] attach 전 기존 program 존재 여부 및 자신의 program ID 기록
- [x] detach 전에 현재 ID와 자신이 붙인 ID 비교
- [x] SIGINT/SIGTERM handler를 attach 전에 준비
- [x] SIGKILL 후 exact owner/program/state 검증 기반 recovery 절차 구현
- [x] 고정 pin namespace는 비어 있을 때만 사용하는 singleton 정책 확정
- [x] `run_id`는 bpffs key/path가 아니라 결과 디렉터리·manifest로 관리
- [x] `BPF_NOEXIST`와 fd 비교로 다른 프로세스의 XSKMAP entry 보호

비정상 종료나 `SIGKILL` 뒤 pin이 남은 경우에는
`xdp_loader --recover --output-dir DIR [IFACE]`를 사용한다. Loader는 attach 전에
고정 map 7개(`xsk_owners` 포함)를 pin하고, 56-byte runtime ABI v4에 interface/capacity, XDP program
ID, owner PID+process start ticks(PID 재사용 방지), 저장/적용 HWTSTAMP와
`APPLYING`/`APPLIED`/`STOPPING` 상태를 기록한다. HWTSTAMP 적용 예정 상태를 ioctl 전에
publish해 ioctl과 완료 map update 사이에 crash해도 복구할 수 있게 했다.

Recovery는 원 owner가 살아 있거나 별도 `xsk_owners` HASH에 살아 있는 receiver
claim이 있으면 거부한다. XSKMAP 자체는 userspace occupancy lookup이 불가능하므로,
receiver claim → runtime 재검증 → XSKMAP 등록과 loader의 STOPPING → claim 검사
순서로 teardown race를 막는다. Owner가 죽었고 현재 Native XDP ID가
저장 ID와 정확히 같을 때만 소유 program을 자동 detach하며, foreign XDP는
건드리지 않고 거부한다. 저장 HWTSTAMP 복구 후 CPUMAP counter를 10 ms
간격으로 최대 5초 검사한다. Requested와 terminal이 같으면 최소 100 ms grace와
10회 연속 안정 snapshot 뒤 export한다. 5초 후에도 차이가 남으면 마지막 1초 이상
변화가 없고 10회 연속 안정됐을 때만 redirect/enqueue loss 가능성인
`unobserved`로 기록하고 export한다. 그 외 timeout은 불완전한 측정으로 실패한다.

기본 recovery는 pin을 유지한다. `--cleanup-pins`는 export와 HWTSTAMP 복구가
모두 성공한 경우에만 pin을 제거하며, 상태 복구 실패는 pin 보존/exit 1로
남긴다. 정상 종료와 recovery 전에 AF_XDP receiver를 먼저 종료해
socket과 `xsk_owners` claim을 해제해야 한다.

- [x] owner/program ID 검증, exact detach, HWTSTAMP 복구 기반 pinned-map recovery/export 구현
- [x] recovery 기본 pin 보존과 명시적 post-export cleanup 구현

## 11. AF_XDP receiver 필수 수정

### 11.1 UMEM address 유실

초기 구현은 RX release 뒤 FILL reserve 실패 시 address를 유실할 수 있었다. 현재
구현은 batch 전체 descriptor를 먼저 검증하고, 같은 개수의 FILL slot을 확보한
뒤에만 recycle/release한다. 일부 reserve는 취소하고 오류로 처리한다.

필요 작업:

- [x] fill ring 공간을 확보한 뒤 RX descriptor release
- [x] 일부 reserve만 성공하면 취소하고 모든 address 보존
- [x] recycle 실패를 치명적 오류로 처리
- [x] batch별 outstanding UMEM frame 소유/recycle invariant 검사
- [ ] 장시간 테스트에서 UMEM 고갈 여부 확인

### 11.2 descriptor address 해석

Descriptor access는 libxdp address helper로 base address와 packet offset을
분리하고 UMEM chunk 범위를 검사한다.

- [x] libxdp XSK address helper 사용
- [x] fill ring에는 chunk base를 반환
- [x] data access에는 packet offset을 적용
- [x] `XDP_PKT_CONTD`/multi-buffer descriptor 명시적 거부

### 11.3 poll 및 socket 통계

- [x] `POLLERR`, `POLLHUP`, `POLLNVAL` 처리
- [x] poll 실패 상태를 종료 code에 보존
- [x] `XDP_STATISTICS`를 `getsockopt()`로 수집
- [x] `rx_dropped`, `rx_ring_full`, `rx_fill_ring_empty_descs`, invalid descriptors 출력
- [x] 실제 NIC RX queue 개수와 CLI queue 인자 비교
- [x] `strtoul()`의 end pointer와 range 검사

### 11.4 AF_XDP copy mode 명시

현재 receiver는 `XDP_COPY`를 사용한다. `XDP_ZEROCOPY`나 driver XSK pool 지원이 없다.

논문에서는 다음을 명확히 표기한다.

- AF_XDP 결과는 copy-mode 결과임
- zero-copy AF_XDP 성능으로 일반화하지 않음
- zero-copy가 필요하면 별도 범위로 `XDP_SETUP_XSK_POOL`, `ndo_xsk_wakeup`, DMA mapping 등을 구현

## 12. Build 및 재현성 보완

### 12.1 BPF build

현재 `BPF/Makefile`의 기본 `make`는 `xdp_loader`와 `afxdp_recv`를 모두 빌드한다.
저장소 최상위 `make`는 BPF와 UDP 도구를 모두 빌드하고, `make test`는 shared
wire/runtime helper ABI, CLI rejection, UDP loopback/flow validation/truncation/strict-HWTS,
join/aggregation/provenance unit test를 외부 NIC나 root 권한 없이 검사한다.

- [x] `all` target에 AF_XDP 포함
- [x] README와 실제 target 일치
- [x] libbpf/libxdp/bpftool dependency와 BPF target architecture 처리
- [x] 측정 기본 quiet와 `--verbose` debug 동작 구분
- [x] 최상위 통합 build와 offline regression target

### 12.2 `vmlinux.h`

`vmlinux.h`는 매 build마다 현재 `/sys/kernel/btf/vmlinux`를 dump한 임시 파일과
비교하고, 내용이 달라진 경우에만 교체한다.

- [x] 현재 `/sys/kernel/btf/vmlinux`에서 매 build 재생성/비교
- [x] manifest에 running kernel release와 BTF hash 기록
- [x] 변경된 `vmlinux.h`에서 skeleton 및 BPF object dependency 재생성

### 12.3 생성물 관리

새로 생성되는 BPF object/skeleton/binary는 `.gitignore`와 `make clean` 대상으로
두고 git에 추적하지 않는 정책으로 확정했다. 다만 이미 tracked인
`BPF/xdp_pass.o`, `BPF/xdp_drop.o`는 사용자의 기존 이력이므로 이번 작업에서
삭제하지 않았다. 이 두 legacy artifact를 언제 이력에서 제거할지는 별도
cleanup 결정으로 남긴다.

- [x] 새/generated BPF 생성물 non-tracking 정책 결정
- [x] `.gitignore`와 `clean` target에 현재 build 생성물 반영
- [ ] 기존 tracked `xdp_pass.o`/`xdp_drop.o` 제거 시점 별도 결정

### 12.4 커널 재현 자료

현재 topic branch는 local-only이며 공식 Raspberry Pi remote branch도 기준점 이후 이동했다. mutable branch 이름만으로는 재현할 수 없다.

보존할 것:

- [x] exact base commit을 manifest에 기록
- [x] 7개 macb commit patch series를 `kernel_commits.patch`로 저장
- [x] manifest에 topic/project HEAD와 dirty status/patch 기록
- [x] kernel `.config` hash 기록
- [x] source/boot kernel image와 installed module tree hash
- [x] target DTB/overlay tree와 boot config/cmdline hash·내용
- [x] compiler/BPF/network/PTP 도구 버전 기록
- [x] BPF object/userspace binary/running BTF hash 기록
- [x] 부팅 cmdline과 CPU isolation runtime 상태 기록
- [x] NIC driver/features/timestamp/stats와 interface 상태 기록
- [ ] raw test logs

`../rt-experiment/thesis-macb-xdp.bundle`이 있더라도 용량이 크고 단독 사용성이 낮으므로, 작은 patch series와 manifest를 함께 보존하는 것이 좋다.

현재 `../rt-experiment/records/`에 있는 일부 config는 `CONFIG_XDP_SOCKETS`가 꺼진 과거 설정이다. 현재 설정 근거로 인용하면 안 된다. 현재 소스 config와 `../rt-experiment/thesis-xdp.config`는 별도로 보존되어 있다.

`monitor/capture_manifest.py`는 기존 파일을 덮어쓰지 않고 `manifest.json`, project와
kernel의 tracked `git diff --binary`와 untracked source raw binary patch/hash, 위
artifact/runtime 정보를 저장한다. `monitor/controller.sh`는
timestamp+PID 고유 `run_id` 디렉터리를 만든 뒤 이 도구를 호출하고,
`ACE_RESULT_ROOT`와 `ACE_INTERFACE`를 실행 환경에서 받는다. `rpi-6.18.y`와의
merge-base, 이후 commit 목록/patch series, 부팅 image·DTB·overlay·module tree도
같은 manifest 묶음에 기록한다.

## 13. 문서에서 정정할 내용

### 13.1 `구현해야할것.md`

다음 항목을 2026-09-10 구현 상태와 확정 정책에 맞게 정정했다.

- target kernel source 위치와 현재 source/runtime 검증 수준
- metadata-valid XDP buffer와 실제 56-byte metadata ABI
- multi-descriptor Native-XDP DROP와 single-descriptor 범위
- `PTP_SYS_OFFSET_EXTENDED` + `CLOCK_MONOTONIC` 보정
- 32-byte 공용 wire header와 `flow_id + sequence` HASH key
- requested path와 실제 sink record의 구분
- Native loader/AF_XDP CLI, quiet hot path, 종료 CSV export
- 일반 UDP HW timestamp와 XDP attach 후 PTP/HWTSTAMP 재확인

### 13.2 `드라이버 포팅한 그 모든것을 담아.md`

- 절 번호가 `11 → 36 → 37 → 14`로 흐트러져 있다.
- `CONFIG_NO_HZ_FULL=y`를 runtime CPU isolation 완료로 표현하면 안 된다.
- XDP_DROP의 `bpftool prog run`은 driver/wire 검증이 아니다.
- XDP_TX와 `ndo_xdp_xmit`은 wire-level 검증 근거가 부족하다.
- 혼합 테스트에는 PASS 경로 최종 consumer 기록이 부족하다.
- `make`만으로 `afxdp_recv`가 생성되는 것처럼 적힌 부분을 고쳐야 한다.
- `CLOCK_MONOTONIC_RAW`와 `CLOCK_MONOTONIC` 설명을 통일해야 한다.

### 13.3 README와 실행 안내

- [x] loader를 `XDP_FLAGS_DRV_MODE` Native로 설명
- [x] AF_XDP가 copy mode임을 명시
- [x] 필수 결과 경로, record capacity, 종료 CSV CLI 반영
- [x] shared packet ABI와 single-descriptor/MTU 정책 반영
- Case A-E와 계획의 C0-C7 명칭을 통일한다.
- [x] `vmlinux.h` 재생성/비교 동작을 실제 Makefile과 일치시킴
- [x] 유한 실험의 최대값을 observed maximum으로 표현
- `적용하는법.md`는 현재로서는 기본 RT 커널 설치 안내이며 논문용 exact environment 재현 문서가 아님을 명시한다.

## 14. 권장 구현 순서

### Phase 0: 재현 기준 고정

- [x] 고유 run directory와 커널/프로젝트 manifest capture 도구 작성
- [x] exact commits/series, config, source·boot image/modules, DT/overlay, cmdline hash 기록
- [x] 현재 기능별 code/build/wire/장시간 검증 수준 분리
- [x] HWTSTAMP와 pinned map recovery/복구 절차 기록

### Phase 1: 현재 경로의 baseline 구축

- [ ] 링크 및 송수신 환경 구성
- [ ] HWTSTAMP RX filter 활성화
- [x] 일반 UDP receiver의 raw hardware timestamp 수집 코드
- [ ] 일반 UDP receiver에서 실제 NIC raw hardware timestamp 확인
- [x] PHC↔MONOTONIC calibration 도구 구현
- [ ] 실제 `/dev/ptpN` calibration 수집
- [ ] XDP 미부착 baseline 측정

### Phase 2: 드라이버 correctness 보완

- [x] RP1 TX kick 공통화
- [ ] XDP_TX/`ndo_xdp_xmit` wire 및 장시간 검증
- [x] multi-descriptor fail-closed DROP/counter 구현
- [ ] jumbo negative test

### Phase 3: XDP timestamp metadata

- [x] descriptor timestamp 공용 helper 분리
- [x] XDP wrapper 추가
- [x] `data_meta` 사용 가능 상태로 초기화
- [x] `xdp_metadata_ops` 연결
- [x] device-bound BPF load
- [ ] metadata helper 성공/실패 검증

### Phase 4: BPF 및 사용자 프로그램 안정화

- [x] packet ID/flow/run ABI 수정
- [x] parser fragment/length/alignment 보완
- [x] AF_XDP UMEM recycle 수정
- [x] AF_XDP statistics 추가
- [x] loader ownership, recovery 및 singleton pin namespace 구현
- [x] hot-path packet별 출력 제거

### Phase 5: 단계별 통합 검증

- [ ] XDP_PASS + UDP
- [ ] CPUMAP + UDP
- [ ] XSKMAP + AF_XDP copy
- [ ] redirect 실패 및 ring-full injection
- [ ] attach/detach 후 PTP 재탐색
- [ ] 장시간 packet conservation 검증

### Phase 6: 최종 실험

- [ ] 동일 patch set에서 non-RT/RT 비교
- [ ] 경로별 단독 부하
- [ ] 혼합 부하
- [ ] 같은 PPS/class ratio의 all-PASS control
- [ ] CPU isolation/IRQ affinity 적용 여부 기록
- [ ] 반복 실험, percentile, observed maximum, deadline miss 분석
- [ ] instrumentation-on/off 대조 실험

## 15. 최소 완료 조건

최종 측정 전에 아래 조건이 모두 충족되어야 한다.

- [x] source commit/dirty binary patch와 source·boot image/modules/DT를 한 manifest에 기록함
- [ ] XDP attach 이후 PHC와 HWTSTAMP 설정을 재확인함
- [ ] 일반 UDP와 XDP가 같은 hardware timestamp 기준을 사용함
- [ ] PHC↔MONOTONIC 보정 오차를 기록함
- [x] `flow_id + sequence` ABI, 필수 flow ID CLI와 외부 run ID 정책 확정
- [x] requested path와 ingress/CPUMAP/AF_XDP/UDP sink record를 분리함
- [ ] AF_XDP에서 UMEM frame 수가 장시간 보존됨
- [ ] CPUMAP/XSK redirect 요청 수와 실제 수신 수의 차이를 설명할 수 있음
- [x] multi-descriptor frame DROP/single-descriptor 범위가 명시되어 있음
- [ ] RP1 XDP TX가 wire와 장시간 부하에서 검증됨
- [ ] CPU isolation과 IRQ affinity가 실제 runtime 상태로 기록됨
- [ ] 모든 결과에 raw log와 환경 manifest가 있음

## 16. 저장소 상태 기록

초기 점검 당시 프로젝트 tracked worktree와 kernel tracked worktree는 clean이었고,
kernel tree에는 기존 untracked patch archive 2개가 있었다. 이후 사용자의 승인에
따라 project와 kernel source 및 이 문서들을 수정했으며 아직 commit하지 않았다.
외부 Ethernet link, 실행 중인 XDP program, HWTSTAMP, IRQ affinity와 boot 설정은
이번 source 구현 과정에서 변경하지 않았다.
