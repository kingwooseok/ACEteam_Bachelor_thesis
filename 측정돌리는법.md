# 측정 돌리는 법과 실험 완료까지 남은 작업

기준일: 2026-09-10.

이 문서는 실험용 수신 아키텍처를 평가하기 위한 진행 안내다.
세 경로의 기본 동작 확인은 끝났고, 다음은 측정 환경을 고정한 뒤 개별 경로와
동시 부하를 비교하는 단계다. 아래 실행 순서는 앞으로 자동화할 절차이며,
현재 한 명령으로 전체 실험을 수행하는 스크립트가 완성됐다는 뜻은 아니다.

## 1. 지금까지 확인한 것

수신기는 포팅한 macb 드라이버가 포함된 Pi 5의
`6.18.46-thesis-test-rt+`, 송신기는 다른 Linux Pi다.
수신 Ethernet은 `eth0`, 수신 주소는 `192.168.10.10`을 사용했다.

공통 조건은 1,000개 송신, 1 ms 주기, `--payload-size 64`였다.
32-byte 실험 헤더까지 포함한 실제 UDP payload는 96 bytes다.
Loader와 AF_XDP receiver의 기록 용량은 `--records 4096`이었다.

| 경로 | UDP 포트 / flow ID | 최초 XDP 기록 | 후속 기록 / 사용자 수신 | 로컬 결과 디렉터리 |
| --- | --- | --- | --- | --- |
| PASS | 9000 / 1 | 1,000 | UDP 1,000 | `results/wire-pass01/` |
| CPUMAP | 9001 / 2 | 1,000 | CPUMAP 1,000, UDP 1,000 | `results/wire-cpumap01/` |
| AF_XDP | 9002 / 3 | 1,000 | AF_XDP 1,000 | `results/wire-xsk01/` |

- 각 경로의 `flow_id + sequence`가 송신 범위와 일치하고 누락·중복이 없었다.
- 세 경로 모두 최초 XDP에서 하드웨어 RX timestamp 1,000개를 얻었다.
- PASS의 socket HW timestamp와 최초 XDP의 HW timestamp가 모두 일치했다.
- CPUMAP 기록은 CPU 3에서 남았고, 최초 XDP → CPUMAP → 사용자 수신의
  MONOTONIC 시각 순서가 모두 맞았다.
- AF_XDP로 전달된 HW timestamp와 최초 XDP timestamp가 원본 기록과 모두
  일치했다. 수신 드롭, metadata 오류, UMEM 관리 오류도 0이었다.
- 종료 후 XDP detach, pin 정리, 기존 HWTSTAMP filter 복원을 확인했다.

CPUMAP의 UDP 출력에서 `missing_hwts=1000`은 이번 구조에서는 예상된 결과다.
CPUMAP이 새로 만드는 SKB에는 socket ancillary HW timestamp를 채우지 않으므로,
분석할 때 같은 packet ID의 최초 XDP HW timestamp를 연결해서 사용한다.
따라서 CPUMAP UDP receiver에는 `--require-hwts`를 넣지 않는다.

AF_XDP의 `tx_ring_empty_descs=2032`는 송신 ring에서 항목을 얻지 못한 횟수이며,
수신 전용인 이번 테스트에서 수신 손실을 의미하지 않는다.
Loader의 `total=1001`, 일반 PASS 1개는 실험 패킷 1,000개 외 트래픽이다.

이 결과는 기능 검증이다. 당시 run에는 실험 전후 시계 보정 자료와 정식
manifest를 함께 수집하지 않았으므로, 보정된 NIC-to-user 성능 결과로 취급하지
않는다. 원본 CSV와 binary는 로컬 `results/`에 보존하고 Git에는 넣지 않는다.

## 2. 측정 순서: 개별 경로부터, 그다음 동시 부하

먼저 같은 패킷 크기·전송 주기·개수로 PASS, CPUMAP, AF_XDP를 각각 측정한다.
한 번 실행하면 세 경로를 순서대로 실행하도록 자동화하면 된다.
세 경로를 동시에 보내는 것은 별도 실험이다.

개별 측정은 경로 자체의 차이를 확인하고, 동시 측정은 공유 NIC와 CPU에서
서로 간섭할 때 분리된 경로가 얼마나 유효한지 확인한다.
각 조건은 여러 독립 run으로 반복하고, 경로 실행 순서도 번갈아 배치한다.
지금의 1,000개 테스트를 그대로 최종 tail latency의 충분한 표본으로 간주하지는 않는다.

## 3. 본 측정 전에 필요한 준비

### 수신 장치 내부 시계 보정

주 측정값은 수신 NIC의 하드웨어 RX 시각부터 사용자 프로그램 수신 시각까지다.
NIC의 PHC 시계와 프로그램의 `CLOCK_MONOTONIC`은 기준이 다르므로 바로 빼면 안 된다.

이미 구현된 `udp_socket/bin/phc_calibrate`를 각 실험 직전과 직후에 실행한다.
두 번 모두 XDP attach와 HWTSTAMP 설정 이후, loader를 종료하기 전에 수행한다.
예를 들어 새 결과 디렉터리 `results/run01/`을 준비하고 loader를 켠 상태에서:

```bash
# 송신을 시작하기 전
sudo ./udp_socket/bin/phc_calibrate \
  --interface eth0 --samples 25 --iterations 10 \
  --output results/run01/phc-monotonic-before.csv

# 송신이 끝나고 남은 수신을 마친 뒤, loader를 끄기 전
sudo ./udp_socket/bin/phc_calibrate \
  --interface eth0 --samples 25 --iterations 10 \
  --output results/run01/phc-monotonic-after.csv
```

분석기는 전후 보정값 사이의 offset을 보간해서 사용한다.
보정 표본이 전체 측정 구간을 앞뒤로 감싸는지와 보정 오차를 확인한다.
전후 자료 없이 나중에 얻은 보정값만으로 과거 run의 정확한 지연을 복원하지 않는다.

이 수신 장치 내부 지연 비교에는 송신 Pi와의 시계 동기화가 필요 없다.
송신 시각부터 수신 시각까지의 장치 간 단방향 지연도 분석하려는 경우에만
송수신 장치 간 시계 동기화를 별도로 준비한다.

### CPU와 실행 조건 고정

NIC IRQ/RX 처리, CPUMAP, UDP/AF_XDP 수신 프로그램, 배경 부하를 각각 어느
CPU에서 실행할지 정하고 실제 affinity·격리 상태·스케줄링 우선순위를 기록한다.
CPU 주파수 정책과 온도에 따른 throttling 여부도 비교 조건에 함께 남긴다.

CPUMAP이 CPU 3에서 실행됐다는 확인만으로 사용자 수신 프로그램까지 CPU 3에
고정됐거나 해당 CPU가 전용으로 격리됐다고 볼 수는 없다.
이전 1,000개 기능 테스트는 격리 효과를 평가한 실험이 아니다.

## 4. 한 run의 실행과 종료 순서

1. 새 결과 디렉터리를 만들고 실험 조건과 환경 manifest를 저장한다.
2. XDP loader를 실행해 attach와 HWTSTAMP 설정을 마친다.
3. 해당 경로의 수신기를 실행하고 CPU 배치·수신 준비를 확인한다.
4. 실험 전 PHC↔MONOTONIC 보정값을 수집한다.
5. 정한 배경 부하를 적용하고 송신기를 실행한다.
6. 송신 종료 후 남은 수신을 마치고 실험 후 보정값을 수집한다.
7. 수신기 기록을 저장하고 종료한다. AF_XDP receiver는 반드시 loader보다 먼저 종료한다.
8. Loader를 정상 종료해 BPF CSV를 저장하고 detach·설정 복원을 마친다.
9. 송신·수신 개수와 drop 통계를 확인하고, 보정값과 packet ID로 기록을 합쳐 분석한다.

결과 파일과 tmpfs 파일은 run별 새 이름을 사용한다.
`--records`는 자동 종료할 패킷 수가 아니라 저장 용량이다. 개별 run에서는
해당 패킷 수, 동시 run에서는 모든 flow를 합친 패킷 수에 맞춰 loader 용량을
정한다. AF_XDP receiver 용량은 AF_XDP flow의 패킷 수에 맞춘다.
Pi의 메모리와 기록 한도를 넘지 않도록 run 크기를 나누면 된다.

현재 [monitor/controller.sh](monitor/controller.sh)는 환경 기록, 부하 실행과
monitoring을 담당한다. 위 loader·수신기·송신기·시계 보정·종료·분석을 연결하는
부분은 아직 남아 있다. 송신 Pi는 TTL에서 별도 송신 스크립트를 실행하는
방식으로 시작해도 되며, 측정 때문에 반드시 SSH 자동 제어를 추가할 필요는 없다.

## 5. 동시 부하에서 비교할 것

- 세 경로에 서로 다른 flow ID로 동시에 송신한다. 모든 수신기가 준비된 뒤 시작한다.
- 먼저 패킷 전송률을 바꾸고, CPU 부하는 별도 축으로 바꾼다. 처음부터 모든
  조건을 한꺼번에 바꾸지 않는다.
- RX 처리를 하는 CPU에 주는 부하와 CPUMAP 목적 CPU에 주는 부하는 구분한다.
  CPUMAP도 최초 NIC 수신 처리는 공유하므로 두 상황의 의미가 다르다.
- 같은 총 PPS·패킷 크기·flow 비율을 유지하면서 모든 패킷을 PASS로 보내는
  대조 실험을 추가한다. 이것이 경로 분리 자체의 효과를 비교할 기준이다.
- 송신 Pi가 설정한 속도를 실제로 낼 수 있는지도 송신 개수와 걸린 시간으로 확인한다.

예를 들어 각 경로에 1,000 pps를 보내면 총 3,000 pps다. 대조군도 총 3,000 pps로
맞춰야 하며, 단독 1,000 pps 결과와 동시 총 3,000 pps 결과를 같은 부하로 취급하지 않는다.
현재 분류기는 포트별 고정 정책이므로 all-PASS 대조 모드는 추가 구현이 필요하다.
본 측정의 공통 조건과 각 flow의 지연 제한(deadline)은 결과를 보기 전에 정한다.

## 6. 분석과 완료 기준

이미 있는 도구는 다음과 같다.

- [join_results.py](analysis/join_results.py): 한 run의 기록과 시계 보정값을 연결한다.
- [aggregate_runs.py](analysis/aggregate_runs.py): 같은 조건의 반복 run을 집계한다.
- [분석 사용법](analysis/README.md): 필요한 manifest·입력 파일과 명령 예시.

현재 join CLI는 UDP binary 한 개만 받고, 요약은 모든 경로를 자동 분리하지 않는다.
동시 실험에서는 PASS/CPUMAP의 여러 UDP 파일을 입력받고 flow/경로별로 지연과
손실을 집계하도록 연결·보완해야 한다. 서로 다른 경로의 수치를 하나의
percentile로 합치지 않는다. 반복 집계와 그래프도 같은 구분을 유지한다.

최종 결과에는 평균·p99·관측 최댓값, 송신/수신 개수·손실률, 정한 deadline을
넘긴 비율을 함께 제시한다. 수신된 패킷의 지연만 낮고 손실이 많아진 경우를
성능 향상으로 해석하지 않는다. 유한한 측정에서 관찰한 최대 지연은 보장된
최악 지연이나 WCET라고 표현하지 않는다.

남은 핵심 작업은 다음 정도로 한정한다.

- [ ] 실제 PHC 보정 수집과 보정된 단일 run 분석을 먼저 끝까지 확인한다.
- [ ] CPU/IRQ 배치와 실행 조건을 고정·기록한다.
- [ ] 개별 경로의 순차 실행·저장·반복 측정을 자동화한다.
- [ ] 동시 트래픽과 all-PASS 대조, CPU/패킷 부하 비교를 실행한다.
- [ ] 경로별 분석과 그래프를 만들고 설계의 효과·한계를 정리한다.
- [ ] 원본 자료 보존, 실행 방법, 코드 버전과 최종 문서 정리를 마친다.

일반 커널과 RT 커널의 비교는 RT 자체의 효과까지 주장할 때 추가한다.
현재 RT 환경에서 경로 분리의 효과를 비교하는 단계와 구분해서 진행한다.
측정 코드 자체의 비용을 주장하려면 instrumentation on/off 비교도 별도 실험으로 둔다.

이번 범위는 VLAN/fragment 없는 IPv4 UDP, MTU 1500의 single-descriptor 패킷과
AF_XDP copy mode다. Multi-descriptor DROP은 구현됐지만 jumbo 실물 검증은
아직 하지 않았으므로 검증 완료로 쓰지 않는다.
수신 아키텍처에 쓰지 않는 XDP_TX 전체 검증, zero-copy 구현, 보안 코너케이스
전반을 이번 측정의 완료 조건으로 추가하지 않는다.
