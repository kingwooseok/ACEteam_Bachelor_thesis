# Hardware RX Timestamp 기반 Deterministic Packet Processing 구현 계획

> 구현 현황 갱신: 2026-09-10
> 아래 설계 중 driver/BPF/UDP userspace에서 외부 Ethernet 없이 구현할 수 있는
> 항목은 현재 workspace에 반영되어 빌드까지 확인했다. 아직 패치 kernel로
> 재부팅하거나 외부 NIC traffic으로 검증한 것은 아니므로, 코드 구현과 실장치
> 검증을 구분해서 읽어야 한다. 최신 보완 체크리스트는 `보충해야할것.md`를
> 함께 참고한다.

## 1. 이 문서의 목적

이 문서는 Raspberry Pi 5의 macb/GEM Native XDP 환경 위에 하드웨어 RX
타임스탬프 측정 기능을 추가한 설계와, 그 값을 CPUMAP/XSKMAP 상위 처리 단계에서
확인하기 위한 현재 구현 상태 및 남은 실장치 검증을 정리한 작업 명세서다.

특히 다음 작업자가 프로젝트를 처음 보더라도 아래 내용을 이해할 수 있도록
구현 이유, 데이터 흐름, 수정 대상, 검증 순서와 주의점을 함께 설명한다.

```text
1. NIC가 packet을 실제로 수신한 시각을 RX descriptor에서 얻는다.
2. Native XDP가 그 시각을 읽을 수 있게 macb driver가 공개한다.
3. XDP program이 timestamp를 packet의 custom metadata에 기록한다.
4. metadata를 CPUMAP 또는 XSKMAP과 함께 이동시킨다.
5. CPU 3의 CPUMAP program 또는 AF_XDP userspace에서 timestamp를 읽는다.
6. 최종 userspace 도착 시각과 결합하여 latency를 계산한다.
7. CPU/network load를 바꾸면서 tail latency와 deadline miss를 비교한다.
```

이 문서는 설계 근거와 남은 검증 계획을 함께 보존한다. 실제 Raspberry Pi target
kernel source는 `~/rt-experiment/linux-6.18-rt`에서 확인했고, driver의 정확한
함수와 patch context에 맞춰 소스 구현을 반영했다. 다만 현재 실행 중인 kernel은
새 소스 변경을 아직 반영하지 않았으므로 runtime 항목은 재부팅 후 확인해야 한다.

---

## 2. 가장 먼저 이해해야 하는 핵심

### 2.1 Native XDP이면 timestamp metadata를 CPUMAP으로 이동할 수 있다

가능하다.

Native XDP가 실행되는 최초 ingress 지점에서 NIC RX descriptor의 hardware
timestamp를 읽고, XDP packet 앞쪽의 `data_meta` 영역에 복사해 두면 그 custom
metadata는 CPUMAP redirect 과정에서 `xdp_frame`과 함께 CPU 3으로 이동할 수
있다.

```text
NIC RX descriptor
    |
    | hardware RX timestamp
    v
macb native XDP context
    |
    | bpf_xdp_metadata_rx_timestamp()
    v
XDP custom data_meta
    |
    | bpf_redirect_map(cpu_map, 3, ...)
    v
CPUMAP queue
    |
    v
CPU 3의 xdp/cpumap program
    |
    +-- data_meta의 hardware timestamp 확인
    +-- CPU 3 도착 시각 추가 기록
    +-- 측정용 BPF map에 결과 기록
    |
    v
XDP_PASS -> SKB -> Linux network stack -> UDP application
```

따라서 CPUMAP에 timestamp 전달이 불가능한 것이 아니다.

정확한 제한은 CPUMAP 이후 `XDP_PASS`로 만들어진 SKB가 custom `data_meta`의
값을 자동으로 `skb_hwtstamps()`에 복사하지 않는다는 점이다. 즉 CPU 3의
`xdp/cpumap` program에서는 metadata를 읽을 수 있지만, 아무 추가 작업도 하지
않으면 일반 UDP application의 `recvmsg()` ancillary data에 hardware timestamp가
자동으로 나타나지는 않는다.

이 프로젝트에서는 이 차이를 다음처럼 처리한다.

```text
CPUMAP까지의 전달
    -> data_meta 사용

CPU 3에서의 확인 및 수집
    -> xdp/cpumap program + preallocated BPF measurement map 사용

최종 UDP application 도착 시각
    -> sequence number 기준으로 userspace record와 offline 결합
```

### 2.2 Hardware timestamp와 software timestamp는 다르다

다음 세 시각은 서로 다른 의미를 가진다.

```text
hw_rx_ns
    NIC/GEM hardware가 frame을 수신하며 RX descriptor에 기록한 시각

xdp_ns
    driver가 XDP program을 실행한 뒤 BPF helper로 읽은 software 시각

user_rx_ns
    userspace가 RX descriptor 또는 UDP datagram을 확인한 뒤 읽은 시각
```

`bpf_ktime_get_ns()`를 XDP에서 호출하는 것만으로도 매우 이른 시각을 얻을 수
있지만, 이것은 NIC hardware timestamp가 아니다. 이번 구현의 핵심은 기존
macb PTP/DMA descriptor timestamp를 Native XDP에 연결하는 것이다.

### 2.3 CPUMAP kernel 자체는 수정하지 않는다

필요한 수정 범위는 다음과 같다.

```text
macb driver
    필요: RX descriptor timestamp를 XDP metadata kfunc으로 공개

XDP BPF program
    필요: timestamp를 data_meta에 저장하고 path별로 redirect

CPUMAP core
    수정 불필요: custom data_meta는 기존 CPUMAP 경로로 이동 가능

AF_XDP receiver
    필요: UMEM packet 앞에 있는 data_meta를 읽고 저장

UDP receiver
    필요: 최종 userspace 도착 시각과 sequence를 저장
```

---

## 3. 현재 프로젝트 상태

### 3.1 Project repository

현재 application/BPF repository는 다음 위치에 있다.

```text
~/ACEteam_Bachelor_thesis
```

주요 파일은 다음과 같다.

```text
BPF/config.h
    port, RT CPU, map 크기, pin 경로 등의 공통 설정

BPF/metadata.h
    XDP metadata, flow_id+sequence key, ingress/CPUMAP/AF_XDP record ABI

include/ace_packet_abi.h
    BPF와 UDP 프로그램이 공유하는 32-byte wire header ABI

BPF/xdp_kern.c
    UDP destination port 기준 Native XDP classifier

BPF/xdp_loader.c
    device-bound BPF load, CPUMAP 설정, Native XDP attach, HWTSTAMP 관리,
    map pin 및 종료 후 CSV export

BPF/afxdp_recv.c
    XSKMAP에 AF_XDP socket을 등록하고 metadata/user timestamp를 preallocated
    record에 저장한 뒤 종료 시 CSV로 내보내는 copy-mode receiver

udp_socket/src/sender.c
    필수 flow ID, sequence와 application TX timestamp를 포함한 UDP packet 송신

udp_socket/src/receiver.c
    일반 UDP socket의 MONOTONIC/REALTIME/HW RX timestamp 저장

udp_socket/src/phc_calibrate.c
    PTP_SYS_OFFSET_EXTENDED를 이용한 PHC↔CLOCK_MONOTONIC calibration CSV 생성

monitor/controller.sh
    고유 run_id/result directory와 manifest를 만든 뒤 CPU 부하, iperf3,
    mpstat, sar를 실행하는 부분 controller

monitor/capture_manifest.py
    exact kernel commit series, dirty tracked/untracked binary patch/hash,
    source·installed artifact와 boot/CPU/NIC/tool 정보를 manifest로 저장

analysis/join_results.py
    manifest-bound full outer join, PHC offset 보간, latency/summary/completion 생성

analysis/aggregate_runs.py
    독립 run 반복 결과의 deadline/percentile/Student-t CI 집계
```

현재 classifier는 다음 정책을 사용한다.

```text
UDP destination port 9001
    -> CPUMAP[3]
    -> CPU 3
    -> Linux network stack

UDP destination port 9002
    -> XSKMAP[RX queue]
    -> AF_XDP userspace

그 외
    -> XDP_PASS
    -> 일반 Linux network stack
```

### 3.2 Target kernel repository

문서상 target kernel source는 별도 workspace에서 관리한다.

```text
~/rt-experiment/linux-6.18-rt
```

기준 정보:

```text
base branch
    Raspberry Pi Linux rpi-6.18.y

working branch
    thesis-macb-xdp

kernel
    PREEMPT_RT

target
    Raspberry Pi 5 / ARM64 / Cadence GEM(macb)
```

기존 driver에는 page pool과 XDP-compatible RX lifecycle, Native XDP,
XDP_REDIRECT, XDP_TX, `ndo_xdp_xmit`이 포팅되어 있고, 과거 기록에는 실제 wire
packet을 이용한 XDP_PASS, CPUMAP, AF_XDP 기능 검증이 남아 있다. 현재 workspace의
추가 timestamp/multi-descriptor/TX-kick 변경은 아직 새 kernel로 부팅해 wire
회귀 검증하지 않았다.

### 3.3 현재 완료된 것

```text
PREEMPT_RT kernel boot
    완료

macb page_pool RX
    완료

macb Native XDP attach
    완료

XDP_PASS / XDP_DROP
    기능 검증 완료

CPUMAP redirect to CPU 3
    tracepoint와 cpumap kthread로 기능 검증 완료

XSKMAP -> AF_XDP userspace
    기능 검증 완료

Mixed traffic 분류
    기능 검증 완료
```

### 3.4 코드는 구현됐지만 target 검증이 남은 것

```text
macb RX descriptor timestamp -> XDP metadata callback
    코드 구현 및 driver object 빌드 완료, 새 kernel boot/wire 검증 필요

XDP data_meta ABI
    56-byte ABI와 32-byte wire ABI 구현 및 build 완료, target 전달 검증 필요

CPUMAP remote XDP program의 metadata 확인
    코드 구현 및 build 완료, target 전달 검증 필요

AF_XDP receiver의 metadata 확인 및 측정 저장
    preallocated 수집/CSV export 구현 및 build 완료, target 장시간 검증 필요

PHC와 system monotonic clock 보정
    calibration 도구 구현 및 build 완료, `/dev/ptpN` 실측 필요

측정용 no-log receiver
    UDP/AF_XDP 모두 packet별 출력 없는 형태로 구현, target 부하 검증 필요

단일 run offline join과 latency summary
    구현 및 unit test 완료

반복 run aggregation과 deadline 분석
    구현 및 unit test 완료, 실제 반복 실험 데이터 수집은 필요
```

---

## 4. 현재 코드에서 먼저 바로잡아야 할 개념과 상태

이 항목들은 timestamp 구현 전에 확인한 위험과 현재 반영 상태다. Source 수정은
반영됐지만 단계별 commit 정리와 target 검증은 남아 있다.

### 4.1 Loader는 Generic XDP가 아니라 Native XDP다

현재 `BPF/xdp_loader.c`의 실제 attach flag는 다음과 같다.

```c
XDP_FLAGS_DRV_MODE
```

따라서 실제 동작은 Native/driver mode다.

Loader의 attach/detach, AF_XDP의 기존 프로그램 검증, 최상위 README 실행 설명을
모두 Native XDP 기준으로 통일했다. Generic/SKB mode fallback은 제공하지 않는다.

### 4.2 현재 AF_XDP는 copy mode다

현재 `BPF/afxdp_recv.c`는 다음 bind flag를 사용한다.

```c
XDP_COPY
```

따라서 현재 검증된 경로는 다음과 같다.

```text
Native XDP
    -> XSKMAP
    -> AF_XDP copy mode
    -> UMEM
```

Native XDP를 사용한다고 AF_XDP가 자동으로 zero-copy가 되는 것은 아니다.
macb driver에 AF_XDP zero-copy용 XSK buffer pool과 wakeup operation 등을
추가하지 않았다면 `XDP_ZEROCOPY`를 사용할 수 없다.

이번 timestamp metadata 구현은 copy mode에서도 가능하다. 따라서 초기 목표에
zero-copy driver 포팅을 섞지 않는다. 논문과 결과에서는 반드시
`AF_XDP copy mode`라고 정확히 표기한다.

Zero-copy는 timestamp 경로와 측정 시스템이 안정화된 뒤 별도 확장 과제로
취급한다.

### 4.3 AF_XDP receiver는 측정용 hot path로 정리했다

현재 receiver는 packet별 `printf()`와 hot-path 파일 I/O를 제거했다. 수신 전에
고정 크기 record 배열을 할당하고 page를 미리 fault-in하며, packet마다
MONOTONIC/REALTIME 관측 시각, wire packet ID, XDP metadata를 배열에만 기록한다.
종료할 때만 CSV를 기록하고 `fsync()`한다.

FILL ring 공간을 RX release 전에 확보하며, batch 전체를 안전하게 recycle하지
못하면 descriptor를 무조건 release하지 않고 오류 처리한다. descriptor address는
base와 data offset을 분리하고 `XDP_PKT_CONTD`는 명시적으로 거부한다. 이 코드의
빌드는 완료됐지만 외부 NIC 고 PPS/장시간 실행에서 UMEM frame 보존 여부는 아직
검증해야 한다.

### 4.4 현재 XDP counter는 redirect 완료 수가 아니다

현재 BPF counter는 `bpf_redirect_map()`이 `XDP_REDIRECT` action을 반환했을 때
증가한다.

그 뒤 실제 enqueue, CPUMAP queue, XSK RX ring 처리 과정에서 drop이 발생할 수
있으므로 이 counter는 다음 의미로 이름을 붙여야 한다.

```text
redirect requested/classified count
```

실제 전달 수는 다음과 함께 판단해야 한다.

```text
CPUMAP tracepoint/drop counter
XDP redirect error
AF_XDP XDP_STATISTICS
NIC statistics
최종 수신 sequence gap
```

---

## 5. 최종 목표 아키텍처

### 5.1 전체 구조

```text
                           Raspberry Pi 5
                    PREEMPT_RT + macb Native XDP

Ethernet wire
    |
    v
Cadence GEM RX DMA
    |
    | descriptor timestamp generated by hardware
    v
macb RX descriptor
    |
    | driver-private XDP context가 descriptor를 참조
    v
xmo_rx_timestamp()
    |
    | bpf_xdp_metadata_rx_timestamp()
    v
Initial XDP classifier
    |
    | data_meta = ace_rx_meta
    |
    +----------------------+----------------------+
    |                      |                      |
    v                      v                      v
XSKMAP                 CPUMAP[3]              XDP_PASS
    |                      |                      |
    v                      v                      v
AF_XDP UMEM          CPU 3 xdp/cpumap       driver-built SKB
    |                      |                      |
    | metadata direct      | metadata read        | skb hwtstamp
    v                      v                      v
AF_XDP app             BPF measurement map     UDP socket
    |                      |                      |
    +----------+-----------+----------------------+
               |
               v
        offline sequence join
               |
               v
   latency / tail / jitter / loss / deadline miss
```

### 5.2 측정하고 싶은 시각

한 packet에 대해 다음 시각을 가능하면 모두 수집한다.

```text
t_hw
    GEM RX descriptor hardware timestamp

t_xdp
    initial XDP program 진입 직후의 monotonic timestamp

t_cpumap
    CPU 3의 xdp/cpumap program 진입 시각

t_user
    AF_XDP RX descriptor 또는 UDP datagram을 userspace가 확인한 시각
```

계산 가능한 구간은 다음과 같다.

```text
driver/NAPI arrival component
    t_xdp - convert(t_hw)

CPUMAP enqueue 및 remote execution component
    t_cpumap - t_xdp

CPU 3 network stack와 application wakeup component
    t_user - t_cpumap

전체 local ingress latency
    t_user - convert(t_hw)
```

AF_XDP path에는 `t_cpumap`이 없으므로 다음처럼 본다.

```text
AF_XDP redirect/ring/userspace component
    t_user - t_xdp
```

### 5.3 연구의 주 metric

최종 논문의 주 metric은 다음으로 정의하는 것이 적절하다.

```text
Local ingress-to-userspace latency
    = userspace가 packet을 확인한 시각
      - NIC/GEM이 packet을 수신한 hardware 시각
```

이 metric은 target 내부의 packet processing architecture를 직접 비교하므로,
sender application scheduling이나 sender kernel TX 지연을 결과에 섞지 않는다.

현재 UDP sender가 기록하는 `CLOCK_REALTIME`의 `tx_ns`는 별도의
application-to-application one-way delay를 계산할 때 사용할 수 있지만, local
hardware RX latency와 같은 이름으로 섞으면 안 된다.

---

## 6. Timestamp metadata ABI 설계

### 6.1 공유 header 구현 상태

Project repository에 다음 파일을 추가했다.

```text
BPF/metadata.h
```

이 header는 최소한 다음 세 코드가 공유한다.

```text
BPF/xdp_kern.c
BPF/afxdp_recv.c
측정 결과 collector 또는 analyzer
```

macb driver는 이 application-specific 구조를 알 필요가 없다. Driver는 오직
hardware timestamp 하나를 kfunc callback으로 제공하고, metadata layout은
BPF program과 userspace 사이의 contract로 유지한다.

### 6.2 확정한 metadata 형식

현재 구현한 native-endian metadata ABI는 다음과 같다.

```c
struct ace_rx_meta {
    __u32 magic;
    __u16 version;
    __u16 size;
    __u32 flags;
    __u32 flow_id;
    __u64 sequence;
    __u64 hw_rx_ns;
    __u64 initial_xdp_ns;
    __u64 cpumap_ns;
    __u32 rx_queue;
    __u16 requested_path;
    __s16 timestamp_error;
};
```

크기는 56 bytes이며 `_Static_assert`로 고정한다.

```c
_Static_assert(sizeof(struct ace_rx_meta) == 56,
               "ace_rx_meta ABI changed");
```

### 6.3 각 field의 의미

```text
magic
    metadata가 실제 ACE format인지 확인하는 고정값

version
    구조가 확장될 때 old/new userspace 혼용을 검출

flags
    HW timestamp, packet ID, CPUMAP 관측, timestamp error,
    requested path 유효 여부

flow_id
    한 run 안에서 송신 flow를 구분하는 값

sequence
    UDP application payload에서 읽은 experiment sequence

hw_rx_ns
    GEM PHC clock domain의 hardware receive timestamp

initial_xdp_ns
    initial XDP program의 bpf_ktime_get_ns() 결과

cpumap_ns
    CPU 3의 xdp/cpumap program이 기록한 시각
    CPUMAP 경로가 아니면 0

rx_queue
    ctx->rx_queue_index

requested_path
    PASS, CPUMAP, XSK 중 classifier가 요청한 path

timestamp_error
    hardware timestamp metadata kfunc의 signed 오류 코드

size
    userspace가 metadata ABI 크기를 검증하기 위한 값이며 현재 56
```

### 6.4 확정한 flag

```c
enum ace_rx_meta_flags {
    ACE_META_F_HWTS_VALID       = 1U << 0,
    ACE_META_F_PACKET_ID_VALID  = 1U << 1,
    ACE_META_F_CPUMAP_SEEN      = 1U << 2,
    ACE_META_F_TIMESTAMP_ERROR  = 1U << 3,
    ACE_META_F_PATH_REQUESTED   = 1U << 4,
};
```

Timestamp callback의 정확한 error code는 `timestamp_error`에 보존하고,
성공/실패 총수는 per-CPU stats map에도 집계한다.

### 6.5 Metadata 크기 변경 규칙

AF_XDP RX descriptor는 metadata 크기를 알려주지 않는다. Userspace는 packet
data 주소에서 고정된 56 bytes만큼 뒤로 이동해서 읽는다.

따라서 다음 규칙을 반드시 지킨다.

```text
1. BPF와 userspace는 같은 metadata.h를 include한다.
2. magic, version, size를 항상 검증한다.
3. field를 재배치하지 않는다.
4. 기존 field 의미를 바꾸지 않는다.
5. ABI 변경 시 version을 증가시킨다.
6. 구조 크기는 4-byte alignment 조건을 만족한다.
```

---

## 7. macb driver 구현 상태와 남은 검증

### 7.1 수정한 파일

Target kernel branch에서 다음 파일을 조사하고 수정했다.

```text
drivers/net/ethernet/cadence/macb.h
drivers/net/ethernet/cadence/macb_main.c
drivers/net/ethernet/cadence/macb_ptp.c
```

Manual porting branch의 실제 함수 인자와 lifecycle에 맞춰 반영했으며, upstream
다른 version에 적용할 때는 다시 context를 확인해야 한다.

### 7.2 기존 timestamp 경로를 먼저 찾는다

기존 일반 SKB RX 경로는 개념적으로 다음과 같다.

```text
RX descriptor 완료
    -> SKB 생성
    -> gem_ptp_do_rxstamp()
    -> gem_ptp_rxstamp()
    -> gem_hw_timestamp()
    -> skb_hwtstamps(skb)->hwtstamp
    -> network stack
```

즉 timestamp를 생성하는 hardware 기능과 descriptor parsing 코드는 이미 있다.
새 구현에서 같은 register/descriptor 해석 코드를 복사해서 두 벌로 만들지
않는다. 기존 코드의 핵심 부분을 공용 helper로 분리한다.

### 7.3 공용 RX descriptor timestamp helper

현재 target source에는 다음 helper를 구현했다.

```c
int gem_ptp_rxstamp_ns(struct macb *bp,
                       struct macb_dma_desc *desc,
                       u64 *timestamp_ns);
```

이 helper가 담당할 일:

```text
1. CONFIG_MACB_USE_HWSTAMP 또는 관련 build option 확인
2. device가 DMA PTP descriptor를 지원하는지 확인
3. RX timestamping이 활성화되어 있는지 확인
4. descriptor의 RX timestamp valid bit 확인
5. 32-bit/64-bit DMA descriptor layout에 맞춰 PTP extension 위치 계산
6. descriptor timestamp word를 timespec64로 복원
7. timespec64를 u64 nanoseconds로 변환
8. 성공 시 0, 실패 시 명확한 negative errno 반환
```

권장 반환 의미:

```text
0
    timestamp 제공 성공

-EOPNOTSUPP
    driver/device/build가 XDP RX timestamp를 지원하지 않음

-ENODATA
    기능은 지원하지만 이 descriptor에 유효 timestamp가 없음

-EINVAL 또는 기타 errno
    descriptor/context 상태가 올바르지 않음
```

기존 `gem_ptp_rxstamp()`도 이 helper를 호출해서 SKB timestamp를 채우도록 하면
Native XDP callback과 일반 SKB 경로가 같은 변환 결과를 사용한다.

### 7.4 Descriptor timestamp를 새로 찍지 않는다

Callback 안에서 단순히 현재 PHC를 읽어 그 값을 RX timestamp라고 저장하면
안 된다.

올바른 값은 다음이다.

```text
NIC/GEM이 RX event에서 descriptor에 저장한 packet timestamp
```

현재 TSU time read는 descriptor가 저장한 seconds 일부를 전체 seconds로
복원하기 위한 보조 정보로만 사용해야 한다. 이것을 NAPI가 packet을 처리한
현재 시각으로 대체하면 hardware ingress latency 측정이라는 의미가 사라진다.

### 7.5 Driver-private XDP context

XDP metadata callback은 실행 중인 packet의 RX descriptor를 알아야 한다.
일반 `struct xdp_buff`에는 macb descriptor pointer가 없으므로 driver-private
wrapper를 구현했다.

개념 예:

```c
struct macb_xdp_buff {
    struct xdp_buff xdp;
    struct macb *bp;
    struct macb_dma_desc *desc;
};
```

중요한 조건:

```text
struct xdp_buff는 첫 번째 member로 둔다.

BPF program 실행 전에 bp와 현재 packet descriptor를 채운다.

callback 실행 동안 descriptor가 recycle되거나 hardware에 반환되지 않아야 한다.

callback은 bpf_prog_run_xdp() 안에서 동기적으로 실행되므로 그 실행 구간에
descriptor가 유효하도록 보장한다.
```

`gem_xdp_run()`은 이 wrapper를 local로 만들고 `&wrapper.xdp`를 기존 XDP
helper에 전달한다.

### 7.6 xdp_metadata_ops callback

개념적인 callback은 다음 책임만 갖는다.

```c
static int gem_xdp_rx_timestamp(const struct xdp_md *ctx, u64 *timestamp)
{
    /* ctx에서 driver-private wrapper를 복원한다. */
    /* 현재 descriptor에 대해 공용 timestamp helper를 호출한다. */
    /* 성공 또는 -errno를 그대로 반환한다. */
}
```

그 뒤 다음 operation table을 준비한다.

```c
static const struct xdp_metadata_ops gem_xdp_metadata_ops = {
    .xmo_rx_timestamp = gem_xdp_rx_timestamp,
};
```

RSS hash나 VLAN metadata는 이번 연구의 필수 범위가 아니다. Timestamp가
검증되기 전 다른 metadata callback을 함께 구현하지 않는다.

### 7.7 net_device에 operation 등록

Device가 실제로 DMA descriptor timestamp를 제공할 수 있을 때 다음 pointer를
등록하도록 구현했다.

```c
netdev->xdp_metadata_ops = &gem_xdp_metadata_ops;
```

등록 시점은 netdev가 사용자에게 공개되기 전에 operation lifetime이 보장되는
probe/setup 경로여야 한다. Operation table은 static lifetime을 갖는다.

현재 등록 조건은 `macb_is_gem()`, `IS_ENABLED(CONFIG_MACB_USE_HWSTAMP)`와
`HW_DMA_CAP_PTP`다. Capability가 없는 MACB variant에는 광고하지 않는다.

```text
macb인가 GEM인가
GEM PTP capability가 있는가
DMA descriptor PTP extension을 지원하는가
CONFIG_MACB_USE_HWSTAMP가 활성화되어 있는가
```

### 7.8 Hardware timestamp enable 상태

Driver callback을 구현해도 hardware RX timestamping이 꺼져 있으면 descriptor에
유효 timestamp가 없을 수 있다.

실험 packet은 일반 UDP 9001/9002이므로 PTP event packet만 timestamp하는 filter가
아니라 다음 설정이 필요하다.

```text
RX filter = HWTSTAMP_FILTER_ALL
```

초기 수동 검증에서는 linuxptp의 `hwstamp_ctl` 또는 동등한 ioctl test program을
사용할 수 있다.

```bash
sudo hwstamp_ctl -i eth0 -r 1
```

최종 loader에는 shell command 대신 `SIOCGHWTSTAMP`/`SIOCSHWTSTAMP`를 직접
사용하는 다음 동작을 구현했다.

```text
1. 기존 hwtstamp config 읽기
2. Native attach로 netdev가 reopen된 뒤 HWTSTAMP_FILTER_ALL 요청
3. driver가 실제로 받아들인 config 재확인
4. Native detach 뒤 기존 config를 명시적으로 재적용
```

`ptp4l`도 같은 interface의 hwtstamp filter를 설정할 수 있으므로 실행 순서에
따라 filter가 PTP-only로 다시 바뀌지 않는지 확인한다.

### 7.9 DMA ordering

XDP program과 timestamp callback이 descriptor의 timestamp word를 읽기 전에
hardware descriptor write가 CPU에 보이는 상태여야 한다.

기존 RX path가 `RX_USED`, control word와 payload를 확인할 때 사용하는 `rmb()`와
`dma_rmb()` 위치를 검토한다. Timestamp word가 control/valid bit 확인 이후에
확실히 보이는지 확인하고, 불필요한 barrier를 중복 추가하지 않는다.

### 7.10 Multi-descriptor packet

이 연구의 Native XDP 범위는 **single-descriptor packet**으로 확정했다. XDP
multi-buffer/frags는 구현하지 않는다. IPv4 MTU는 1500, UDP payload는 최대
1472 bytes이고 sender는 DF(`IP_PMTUDISC_DO`)를 설정한다.

Driver는 Native XDP가 붙어 있는 동안 `SOF && EOF`가 아닌 RX frame을 일반 SKB
조립 경로로 보내지 않는다. frame의 descriptor chain을 NAPI poll 경계를 넘어
EOF까지 drain/recycle하고 전체 frame을 DROP하며, frame당 한 번
`rx_xdp_multidesc_drops`를 증가시킨다. 이 fail-closed 처리는 소스 구현과 driver
object 빌드까지 완료했다.

남은 항목은 외부 Ethernet에서 정상 MTU packet이 영향받지 않는지 확인하고,
의도적으로 jumbo/multi-descriptor frame을 넣어 DROP과 counter 증가를 검증하는
negative test다. 따라서 multi-descriptor frame의 timestamp descriptor를 고르는
문제는 현재 논문 범위에 없다.

### 7.11 XDP_PASS regression 방지

Native XDP가 `XDP_PASS`를 반환한 뒤 driver가 SKB를 만드는 경로에서도 기존
hardware timestamp가 계속 `skb_hwtstamps()`에 설정되는지 확인한다.

다음 두 결과가 같은 packet에 대해 합리적으로 일치해야 한다.

```text
XDP kfunc이 반환한 hw_rx_ns
SKB SO_TIMESTAMPING_RAW_HARDWARE가 반환한 timestamp
```

완전히 동일한 descriptor source를 사용한다면 두 값은 동일해야 한다. 차이가
있다면 descriptor 선택, timestamp enable 상태 또는 clock 변환 경로를 점검한다.

### 7.12 Driver 구현에서 하지 말아야 할 것

```text
Driver가 ACE application-specific metadata layout을 직접 작성하지 않는다.

Packet payload에 timestamp를 삽입하지 않는다.

Timestamp를 얻기 위해 SKB를 먼저 생성하지 않는다.

기존 gem_hw_timestamp()와 별도의 중복 변환 코드를 만들지 않는다.

모든 MACB variant가 timestamp를 지원한다고 가정하지 않는다.

Timestamp valid가 없는 descriptor에 임의의 현재 시각을 넣지 않는다.
```

---

## 8. XDP BPF program 구현 상태와 남은 검증

### 8.1 Timestamp kfunc declaration

`BPF/xdp_kern.c`에서 kernel BTF kfunc을 참조하도록 선언한다.

개념 예:

```c
extern int bpf_xdp_metadata_rx_timestamp(const struct xdp_md *ctx,
                                         __u64 *timestamp) __ksym;
```

실제 선언은 target kernel의 selftest 및 libbpf header와 동일한 signature인지
확인한다.

### 8.2 처리 순서

권장 initial XDP 처리 순서는 다음과 같다.

```text
1. t_xdp = bpf_ktime_get_ns()를 가능한 초기에 읽음
2. Ethernet/IPv4/UDP header bounds check
3. destination port로 path 결정
4. experiment payload에서 sequence 읽기
5. bpf_xdp_adjust_meta(-sizeof(struct ace_rx_meta))
6. data와 data_meta pointer를 다시 읽음
7. metadata bounds check
8. magic/version/size/기본 field 초기화
9. bpf_xdp_metadata_rx_timestamp() 호출
10. 성공 여부와 hw_rx_ns 기록
11. path별 redirect 또는 pass
```

`bpf_xdp_adjust_meta()`를 호출하면 verifier 관점의 packet pointer가 바뀔 수
있으므로 기존 `data`, `data_end`, `data_meta`를 그대로 재사용하지 않고 다시
읽고 bounds check한다.

### 8.3 data_meta 할당

개념 예:

```c
if (bpf_xdp_adjust_meta(ctx, -(int)sizeof(struct ace_rx_meta))) {
    count metadata allocation failure;
    follow strict measurement failure policy;
}

data = (void *)(long)ctx->data;
data_meta = (void *)(long)ctx->data_meta;

if (data_meta + sizeof(struct ace_rx_meta) > data) {
    count metadata bounds failure;
    follow strict measurement failure policy;
}
```

Metadata 크기는 XDP가 허용하는 범위와 현재 macb driver가 제공하는 headroom
안에 들어가야 한다. 현재 Native XDP port가 `XDP_PACKET_HEADROOM`을 확보한다면
48-byte metadata는 일반적으로 충분히 작지만, target에서 실제
`data_hard_start`, `data_meta`, `data` 간격을 확인한다.

### 8.4 Timestamp error 처리

다음 두 mode를 구분하면 개발과 측정에 유리하다.

```text
bring-up/debug mode
    timestamp가 없어도 packet path를 계속 진행
    flags와 error counter로 실패 이유 기록

strict measurement mode
    metadata 또는 timestamp가 없는 sample을 정상 sample로 취급하지 않음
    drop/error counter 증가
    run 종료 시 실패 처리
```

어떤 mode에서도 timestamp가 유효하지 않은데 0을 정상 값처럼 저장하면 안 된다.
`ACE_META_F_HWTS_VALID`가 설정된 record만 latency 계산에 사용한다.

### 8.5 Sequence 추출

UDP sender, receiver와 BPF parser는 `include/ace_packet_abi.h`의 다음 32-byte
wire header를 공유한다. 모든 field는 network byte order다.

```c
struct ace_experiment_header {
    __be32 magic;
    __be16 version;
    __be16 size;
    __be32 flow_id;
    __be32 reserved;
    __be64 sequence;
    __be64 tx_realtime_ns;
};
```

XDP program은 UDP/IP 실제 길이와 `data_end`를 모두 확인하고, unaligned payload를
bounded copy한 뒤 magic/version/size/reserved, `flow_id`, `sequence`를 검증한다.

`flow_id + sequence`를 metadata 및 measurement map의 key로 사용하면 initial XDP,
CPUMAP remote program, AF_XDP receiver, UDP receiver의 record를 offline에서
정확히 결합할 수 있다.

`run_id`는 wire packet과 map key에 넣지 않는다. `results/<run-id>/` 결과
디렉터리와 별도 manifest가 run 경계를 제공하며 run마다 새 map을 사용한다.
UDP sender/receiver의 `--flow-id`는 필수이며, 한 loader run에서 동시에 보내는
각 flow/path에는 서로 다른 값을 배정한다.

실험 port로 들어왔지만 header 길이가 부족한 packet은 별도 invalid counter에
기록한다.

### 8.6 Parser 범위

초기 연구에서는 실험 조건을 단순하게 고정한다.

```text
Ethernet II
IPv4
UDP
fragmentation 없음
VLAN 없음
고정된 experiment packet header
```

VLAN이나 IPv4 fragment를 지원하지 않을 경우 문서와 실행 전 검증에서 명시한다.
특히 fragmented UDP의 첫 fragment와 나머지 fragment가 서로 다른 path로 갈 수
있으므로 실험 traffic에서는 fragmentation을 금지하거나 classifier가 명시적으로
fragment를 PASS/DROP 처리해야 한다.

### 8.7 Path enum

Metadata와 결과 파일이 같은 값을 사용하도록 path enum을 공유한다.

```c
enum ace_packet_path {
    ACE_PATH_PASS = 0,
    ACE_PATH_CPUMAP = 1,
    ACE_PATH_XSK = 2,
};
```

통계 map의 기존 index와 동일한 숫자를 사용하더라도 의미를 주석으로 명확하게
고정한다.

### 8.8 초기 XDP measurement record

최종 측정에서는 모든 path에 동일한 initial instrumentation overhead가 들어가야
한다. 따라서 initial XDP에서 모든 실험 packet에 대해 같은 metadata helper와
같은 형태의 ingress record 작성을 수행한다.

구현한 측정 map:

```text
ingress_records
    BPF_MAP_TYPE_HASH
    key = struct ace_packet_key { flow_id, reserved, sequence }
    value = hw_rx_ns, initial_xdp_ns, path, queue, flags
```

Loader는 object load 전에 `bpf_map__set_max_entries()`로 HASH capacity를 설정한다.
기본값은 UDP 기본 200,000 packet run을 수용하도록 여유를 둔 262,144개이고,
CLI `--records` 범위는 1~1,048,576이다. `LRU_HASH`는 자동 축출을 packet loss와
혼동할 수 있어 사용하지 않는다. packet마다 ring-buffer consumer를 깨우지 않고
종료 후 map을 순회해 CSV로 저장한다.

다음 조건을 둔다.

```text
flow_id + sequence를 key로 BPF_NOEXIST 기록
duplicate와 map update error를 별도 집계
run마다 빈 pin namespace에서 새 map 생성
측정 종료 후에만 userspace가 전체 record를 읽음
```

### 8.9 Instrumentation overhead control

BPF map write와 timestamp kfunc 호출 자체도 비용이 있다. 다음 두 control run을
추가한다.

```text
Native XDP classifier only
    timestamp metadata와 measurement map write 비활성

Native XDP classifier + timestamp instrumentation
    실제 측정 기능 활성
```

두 run의 차이를 기록하면 관측 장치가 datapath에 추가한 비용을 설명할 수 있다.

---

## 9. CPUMAP 경로 구현 상태와 남은 검증

### 9.1 Remote XDP program을 사용한다

CPUMAP은 kernel 5.9 이후 destination CPU에서 두 번째 XDP program을 실행할 수
있다. 이 project의 6.18 kernel에서는 이를 사용할 수 있다.

현재 구현한 remote program은 다음 section을 사용한다.

```c
SEC("xdp/cpumap")
int xdp_cpumap_measure(struct xdp_md *ctx)
```

이 program의 역할:

```text
1. ctx->data_meta와 ctx->data bounds 확인
2. ace_rx_meta magic/version/size 확인
3. metadata에서 hw_rx_ns와 initial_xdp_ns 확인
4. bpf_ktime_get_ns()로 cpumap_ns 기록
5. ACE_META_F_CPUMAP_SEEN flag 설정
6. cpumap_records[sequence]에 record 저장
7. XDP_PASS 반환
```

이 program에서는 원래 NIC descriptor를 다시 읽을 수 없다. 그러나 initial XDP가
미리 복사한 `data_meta`는 읽을 수 있으므로 hardware timestamp kfunc을 다시
호출할 필요가 없다.

### 9.2 CPUMAP value layout 변경

`cpu_map` value를 queue size만 담는 `__u32`에서 remote program FD를 포함하는
다음 BTF layout으로 변경했다.

```c
struct {
    __uint(type, BPF_MAP_TYPE_CPUMAP);
    __uint(max_entries, ...);
    __type(key, __u32);
    __type(value, struct bpf_cpumap_val);
} cpu_map SEC(".maps");
```

Loader는 다음 값을 채운다.

```c
struct bpf_cpumap_val value = {
    .qsize = configured_queue_size,
    .bpf_prog.fd = remote_cpumap_program_fd,
};
```

### 9.3 CPUMAP record는 별도 map에 쓴다

Initial XDP와 remote CPUMAP program이 같은 record를 동시에 갱신하지 않도록
다음처럼 HASH map을 분리했다.

```text
ingress_records[flow_id, sequence]
    initial XDP만 작성

cpumap_records[flow_id, sequence]
    remote xdp/cpumap program만 작성

userspace_records[flow_id, sequence]
    UDP receiver가 작성
```

실험 종료 후 세 record를 `flow_id + sequence`로 결합한다.

### 9.4 CPUMAP kthread와 RT scheduling

CPUMAP entry를 CPU 3으로 설정하는 것은 CPU 3으로 packet work를 보낸다는
뜻이지, 그 kthread가 자동으로 높은 RT priority를 갖는다는 뜻은 아니다.

Target에서 다음 정보를 확인하고 저장한다.

```bash
ps -eLo pid,tid,cls,rtprio,pri,psr,comm | grep -i cpumap
```

필요하면 experiment setup에서 cpumap worker의 scheduling policy와 priority를
명시적으로 설정하고, 모든 비교 run에서 같은 정책을 유지한다.

다음 두 task가 CPU 3에서 서로 어떤 priority로 동작하는지도 정의해야 한다.

```text
cpumap/3/map:<id> kernel thread
UDP receiver userspace RT task
```

### 9.5 CPUMAP queue size

현재 queue size는 256이다. Queue가 크면 burst tolerance는 좋아질 수 있지만
queueing delay의 worst observed value가 커질 수 있다. Queue가 작으면 latency는
제한될 수 있지만 drop이 빨리 발생할 수 있다.

따라서 256을 고정된 진리로 사용하지 말고 다음과 같이 별도 실험 변수로 본다.

```text
qsize = 64, 128, 256, 512 등
```

단, 첫 성능 비교에서는 변수를 한꺼번에 늘리지 말고 256 하나로 전체 구조를
검증한 뒤 queue-size sweep을 별도 실험으로 수행한다.

---

## 10. AF_XDP 경로 구현 상태와 남은 검증

### 10.1 Metadata 위치

AF_XDP RX descriptor의 `addr`는 packet data를 가리킨다. Custom metadata는
packet data 바로 앞에 있다.

개념:

```text
UMEM frame

+-----------+----------------------+-------------------+
| headroom  | struct ace_rx_meta   | Ethernet packet   |
+-----------+----------------------+-------------------+
            ^                      ^
            |                      |
            metadata               RX descriptor addr
```

Userspace에서는 다음 순서로 접근한다.

```text
1. RX descriptor addr의 base/offset를 libxdp helper로 정규화
2. packet data pointer 계산
3. data - sizeof(struct ace_rx_meta)로 metadata pointer 계산
4. UMEM frame 범위 확인
5. magic/version/size/flags 확인
6. HWTS_VALID인 경우에만 timestamp 사용
```

### 10.2 Address helper를 올바르게 사용한다

AF_XDP descriptor address에는 chunk address와 offset 정보가 포함될 수 있으므로
현재 metadata와 frame access에 다음 libxdp helper를 사용한다.

```text
xsk_umem__extract_addr()
xsk_umem__add_offset_to_addr()
xsk_umem__get_data()
```

Packet data 접근용 address와 FILL ring으로 되돌릴 frame base address를 구분한다.
현재 단순한 raw `desc->addr` 계산이 target mode에서 우연히 동작하더라도 metadata
및 향후 unaligned mode에 안전하도록 정식 helper 기준으로 정리한다.

### 10.3 측정용 receiver hot path

현재 receiver hot path는 다음 작업만 수행한다.

```text
RX ring batch peek
    -> descriptor/data/meta bounds check
    -> user timestamp 읽기
    -> sequence/meta/timestamp를 preallocated record에 복사
    -> frame address를 FILL ring에 반환
    -> RX ring release
```

Hot path에서 금지할 것:

```text
packet별 printf
malloc/free
파일 write/fsync
동적 container resize
mutex를 이용한 다중 consumer
DNS 또는 문자열 변환
```

### 10.4 FILL ring 고갈 방지

현재 구현은 처리한 RX descriptor를 release하기 전에 batch 전체 frame 주소를
FILL ring에 다시 제출할 수 있는지 검증한다.

Reserve가 일부 또는 전부 실패한 경우:

```text
주소를 잃어버리지 않는다.
pending recycle queue에 보관하거나 즉시 retry한다.
recycle failure를 counter에 기록한다.
수신 descriptor를 무조건 release하지 않는다.
```

최종적으로 다음 invariant를 검사한다.

```text
initial fill frames
    = kernel-owned + RX-ring-owned + userspace-owned + pending-recycle frames
```

### 10.5 Poll mode와 busy-spin mode를 구분한다

`poll()` 기반 수신은 CPU 사용량은 줄지만 scheduler wakeup latency를 포함한다.
Busy-spin은 CPU core를 계속 사용하지만 가장 낮은 userspace response latency를
목표로 할 수 있다.

두 mode를 섞어 결과를 비교하지 않는다.

```text
AF_XDP-poll
AF_XDP-busy
```

현재 receiver는 `poll()` mode만 구현했다. 이를 최초 논문 비교의 primary로
사용하고, busy-spin을 추가한다면 별도 case로 제시한다. Busy-spin application
core에는 다른 workload를 배치하지 않는다.

### 10.6 Copy mode를 명시한다

결과 파일과 run manifest에 다음을 반드시 저장한다.

```text
AF_XDP bind mode = COPY
```

향후 zero-copy가 구현되면 kernel이 실제로 `XDP_OPTIONS_ZEROCOPY`를 보고하는지
확인하고 다음처럼 완전히 다른 case로 취급한다.

```text
AF_XDP copy
AF_XDP zero-copy
```

---

## 11. XDP_PASS 및 일반 UDP socket 경로

### 11.1 기존 SKB hardware timestamp 확인

XDP_PASS는 macb driver가 직접 SKB를 만들기 때문에 기존
`gem_ptp_do_rxstamp()` 경로를 유지하면 socket에서 hardware timestamp를 받을 수
있다.

현재 UDP receiver는 다음 API로 raw hardware RX timestamp를 수집하며,
`--require-hwts`에서 누락을 run 실패로 처리하되 partial record를 보존한다.

```text
SO_TIMESTAMPING
SOF_TIMESTAMPING_RX_HARDWARE
SOF_TIMESTAMPING_RAW_HARDWARE
recvmsg() control message
```

Raw hardware timestamp는 일반적으로 timestamp 배열의 hardware slot에서
확인한다. 실제 structure와 flags는 target kernel UAPI에 맞춰 구현한다.

### 11.2 CPUMAP과의 차이

XDP_PASS에서는 driver가 descriptor를 가지고 SKB를 만들기 때문에
`skb_hwtstamps`를 채울 수 있다.

CPUMAP에서는 initial driver가 packet을 redirect하고, 나중에 generic CPUMAP
code가 CPU 3에서 SKB를 만든다. 이 시점에는 원래 macb descriptor가 없으므로
custom metadata가 자동으로 `skb_hwtstamps`가 되지 않는다.

그러므로 CPUMAP 결과는 다음 방식으로 모은다.

```text
BPF ingress/cpumap record
    +
UDP userspace record
    +
flow_id + sequence offline join
```

### 11.3 UDP receiver timestamp boundary

현재 receiver는 `recvmsg()`가 반환된 직후 `CLOCK_MONOTONIC`, 이어서
`CLOCK_REALTIME`을 읽고 ancillary raw HW timestamp를 decode한다. Packet별 출력,
allocation과 파일 I/O는 hot path에 없다.

권장 primary boundary:

```text
recv 또는 recvmmsg가 packet을 userspace에 반환
    -> 즉시 CLOCK_MONOTONIC 읽기
```

향후 `recvmmsg()` batch를 추가하면 각 packet의 실제 application observation
시각이 batch 단위로 뭉칠 수 있으므로 별도 throughput-oriented case로 둔다.

---

## 12. Loader 구현 상태와 남은 검증

### 12.1 Device-bound BPF load

`bpf_xdp_metadata_rx_timestamp()` 같은 device-bound kfunc를 사용하는 program은
어느 netdev의 driver callback을 호출할지 load 시점에 알아야 한다.

따라서 기존 단일 `xdp_kern__open_and_load()` 호출 대신 다음 순서를 구현했다.

```c
xdp_kern__open_and_load();
```

```text
1. interface name -> ifindex
2. xdp_kern__open()
3. initial XDP program에 bpf_program__set_ifindex(ifindex)
4. initial program에 BPF_F_XDP_DEV_BOUND_ONLY 설정
5. measurement map max_entries 설정
6. xdp_kern__load()
7. CPUMAP value와 runtime ABI v4(owner/program/HWTSTAMP/STOPPING 상태) 설정
8. 고정 map 7개(`xsk_owners` 포함)를 attach 전 pin
9. Native/DRV mode attach
10. netdev reopen 뒤 HWTSTAMP_FILTER_ALL 적용 및 runtime 상태 갱신
```

Remote `xdp/cpumap` program에는 원래 descriptor metadata kfunc를 호출하지 않는다.
Device-bound flag가 어느 program에 필요한지 program별로 설정한다.

### 12.2 Native mode 일관성

다음 항목을 모두 Native 기준으로 맞췄다.

```text
xdp_loader attach/detach flag
loader log message
README 실행 설명
AF_XDP configuration comment
runtime verification command
```

AF_XDP socket은 BPF program auto-load를 금지하고 loader가 pin한 XSKMAP을
사용한다. Socket `xdp_flags`도 `XDP_FLAGS_DRV_MODE`로 검증해 별도 Generic
program을 attach하지 않는다.

### 12.3 기존 program 보호

실험 interface에 다른 Native XDP program이 있으면 시작을 거부한다. Attach에는
`XDP_FLAGS_UPDATE_IF_NOEXIST`를 사용하고, 종료 시 현재 program ID와 loader가
기록한 ID를 대조해 자신이 소유한 program만 detach한다. SIGINT/SIGTERM handler는
attach 전에 설치한다.

```text
attach 전에 현재 program ID 확인
UPDATE_IF_NOEXIST로 기존 program 보호
종료 시 현재 붙은 program이 자신의 program인지 확인 후 detach
```

### 12.4 Pin lifecycle

Loader가 전체 run의 owner이며 `/sys/fs/bpf/ace_xdp`가 비어 있지 않으면 새 run을
거부한다. 정상 종료 순서는 program detach → CPUMAP drain → record export →
저장한 HWTSTAMP 설정의 명시적 재적용 → unpin이다. Map은 attach 전에 pin되며,
56-byte runtime ABI v4에 interface/record capacity, XDP program ID, owner PID와
process start ticks(PID 재사용 방지), 저장/적용 HWTSTAMP 설정과
`APPLYING`/`APPLIED`/`STOPPING` 상태를 기록한다. 적용 예정 상태를 ioctl 전에
publish하여 ioctl과 완료 map update 사이에 crash해도 saved/effective 둘 중
현재 상태를 확인해 복구할 수 있다.

CPUMAP drain은 10 ms 간격으로 최대 5초 검사한다. Requested와 terminal
record/error 수가 같으면 최소 100 ms grace와 10회 연속 안정 snapshot 뒤
export한다. 5초 시점에 차이가 남아도 마지막 1초 이상 변화가 없고 10회
연속 안정됐을 때만 redirect/enqueue loss 가능성인 `unobserved`로 기록하고
export한다. 그 외 timeout과 detach/export/HWTSTAMP 복구 실패에서는 pin을
보존한다.

비정상 종료/강제 종료 뒤의 보존 pin은 다음 CLI로 복구할 수 있다.

```bash
xdp_loader --recover --output-dir DIR [IFACE]
xdp_loader --recover --cleanup-pins --output-dir DIR [IFACE]
```

Recovery는 pin schema/runtime identity를 검증하고 loader owner가 아직 살아
있거나 `xsk_owners`에 살아 있는 AF_XDP receiver claim이 있으면 거부한다.
XSKMAP은 userspace occupancy lookup을 지원하지 않으므로 receiver가 queue claim을
XSKMAP 등록 전에 만들고, loader는 `STOPPING` publish 뒤 claim을 검사한다. Owner가 죽었고
현재 Native XDP ID가 저장 ID와 정확히
같을 때만 소유 program을 자동 detach한다. 다른 ID의 program은 foreign XDP로
보고 건드리지 않으며 복구를 거부한다. 이어서 저장 HWTSTAMP를 복구하고
위 drain 정책을 통과한 뒤 CSV를 export한다. 기본은 pin을 유지하고,
`--cleanup-pins`는 export와 상태 복구가 모두 성공한 뒤에만 pin을 제거한다.
복구 실패는 pin 보존/exit 1로 남긴다. 정상 종료와 recovery 전에 AF_XDP
receiver를 먼저 종료해 socket과 queue claim을 해제해야 한다.

```text
누가 pin directory의 owner인가?
stale pin이 있으면 새 run이 실패하는가?
loader가 비정상 종료하면 cleanup은 어떻게 하는가?
collector가 map을 읽는 동안 loader가 종료될 수 있는가?
```

```text
loader start
    -> maps/program ready
receiver start
    -> measurement
receiver stop and data collected
loader stop
    -> detach/unpin/restore hwtstamp
```

### 12.5 Measurement 중 loader 출력

Loader는 기본 quiet mode라 측정 중 통계를 출력하지 않는다. `--verbose`는 debug
run에서만 1초마다 통계를 출력한다. 최종 run에서는 종료 후 한 번만 읽고 CSV로
저장하며, 필요하면 loader 자체도 housekeeping CPU에 고정한다.

```text
measurement quiet mode
또는
loader를 housekeeping CPU에 고정
```

실시간 통계 출력은 debug run에서만 사용하고 최종 run에서는 종료 후 한 번만
읽는 것이 좋다.

---

## 13. Measurement map 및 결과 파일 설계

### 13.1 BPF ingress record

현재 `BPF/metadata.h`의 구현:

```c
struct ace_ingress_record {
    __u32 flow_id;
    __u32 rx_queue;
    __u64 sequence;
    __u64 hw_rx_ns;
    __u64 initial_xdp_ns;
    __u32 flags;
    __u16 requested_path;
    __s16 timestamp_error;
};
```

### 13.2 CPUMAP record

```c
struct ace_cpumap_record {
    __u32 flow_id;
    __u32 cpu;
    __u64 sequence;
    __u64 cpumap_ns;
    __u32 flags;
    __u16 requested_path;
    __u16 reserved;
};
```

Remote program에서는 `bpf_get_smp_processor_id()`가 실제로 CPU 3인지 기록하고,
CPU 3이 아닌 record를 configuration failure로 처리한다.

### 13.3 Userspace record

```c
struct ace_xsk_user_record {
    __u32 flow_id;
    __u32 rx_queue;
    __u64 sequence;
    __s64 tx_realtime_ns;
    __u64 user_rx_mono_ns;
    __s64 user_rx_real_ns;
    __u64 hw_rx_ns;
    __u64 initial_xdp_ns;
    __u32 meta_flags;
    __u16 requested_path;
    __s16 timestamp_error;
    __u32 packet_length;
    __u32 validation_flags;
};
```

AF_XDP receiver는 이 72-byte in-memory record를 종료 시 CSV로 내보낸다.

### 13.4 Final joined record

Offline analyzer가 만드는 최종 형식 예:

```c
struct ace_joined_record {
    __u32 flow_id;
    __u64 sequence;
    __u64 hw_rx_ns;
    __u64 hw_rx_mono_ns;
    __u64 initial_xdp_ns;
    __u64 cpumap_ns;
    __u64 user_cpumap_ns;
    __u64 user_rx_ns;
    __u32 packet_length;
    __u16 requested_path;
    __u16 flags;
};
```

Raw record는 수정하지 않고 보존한다. Clock conversion 결과는 별도 field 또는
별도 processed file로 생성한다.

모든 offline join key는 `flow_id + sequence`다. `run_id`는 결과 디렉터리와
manifest에서 관리하므로 joined row의 packet key에 포함하지 않는다.

현재 `analysis/join_results.py`는 `--manifest`, ingress, 하나 이상의
`--phc-calibration`을 필수로 하고 CPUMAP, AF_XDP, native UDP record를
선택 입력으로 받아 full outer join한다. Manifest version/`run_id`를 확인하고
모든 입출력이 해당 resolved run directory 안에 있는지 검증한다.

Join 시 requested path, metadata/validation flag, timestamp/error 조합,
ingress-CPUMAP/AF_XDP/UDP sink 일치를 의미적으로 검증한다. 모순된 raw row는
`inconsistency_flags`로 보존하되 관련 metric에서 제외한다. Joined CSV에는
source별 present/raw field와 `xdp-hw`, `cpumap-xdp`, `user-cpumap`, `user-xdp`,
`user-hw`를 남긴다. `user-cpumap`은 remote CPUMAP BPF 관측 이후부터
userspace 관측까지의 합성 구간이며 순수 queue/socket 지연이 아니다.

선택된 PHC calibration 표본 사이 offset은 선형 보간한다. 범위 밖 timestamp의
clock-crossing metric은 기본적으로 제외하고 out-of-range 위치/거리로
집계하며, 명시적 `--allow-endpoint-clamp`는 탐색적 분석에서만 endpoint
offset을 사용한다. 최종 측정에서는 run 직전/직후 calibration selected
범위가 전체 측정 구간을 bracket해야 한다. Summary JSON에는 입력별
duplicate/conflict/missing/inconsistency, 음수 latency와
count/min/mean/median/p95/p99/observed maximum을 기록한다.

Joined CSV와 summary JSON을 `O_EXCL`/`fsync()`로 완료한 뒤 모든 입력과
두 출력의 크기/SHA-256을 묶은 `summary.json.complete` marker를 마지막에 만든다. 반복 run
분석은 `analysis/aggregate_runs.py`에 completion marker만 넘긴다. 이 도구는
매니페스트 parameter와 interface/kernel/source/artifact cohort, 분석 정책이 같은
최소 2개의 run만 묶는다. 명시적 deadline에 대한 pooled 통계와, 각 독립
run의 p50/p95/p99·miss rate를 동일 가중한 평균의 양측 95% Student-t
신뢰구간을 출력한다. Pooled packet 통계에는 CI를 붙이지 않는다.

### 13.5 Record validity

다음 record는 정상 latency sample에 포함하지 않는다. 다만 삭제하지 않고 실패
이유와 함께 보존한다.

```text
HW timestamp invalid
metadata magic/version mismatch
sequence 범위 초과
duplicate sequence
clock calibration 범위 밖
user record만 있고 ingress record가 없음
ingress record만 있고 user record가 없음
CPUMAP path인데 CPUMAP_SEEN이 없음
```

이들을 단순 outlier로 제거하지 말고 loss, instrumentation failure 또는 path
failure로 각각 집계한다.

---

## 14. Clock domain 설계

### 14.1 직접 빼면 안 되는 시각

GEM descriptor timestamp는 PHC domain이다. BPF의 `bpf_ktime_get_ns()`와
userspace의 `CLOCK_MONOTONIC`은 system monotonic domain이다.

```text
PHC timestamp - MONOTONIC timestamp
```

를 offset 보정 없이 직접 계산하면 의미가 없다.

### 14.2 권장 clock 선택

Stage 분해를 위해 다음 조합을 권장한다.

```text
hardware RX
    GEM PHC

initial XDP / CPUMAP
    bpf_ktime_get_ns()

userspace
    CLOCK_MONOTONIC
```

`bpf_ktime_get_ns()`와 userspace clock의 정확한 대응은 target kernel에서 간단한
validation test로 확인한다.

### 14.3 PHC와 MONOTONIC 보정

Linux 6.18에서는 `PTP_SYS_OFFSET_EXTENDED`를 이용해 PHC read 전후의 system
clock을 함께 얻을 수 있다.

권장 calibration 절차:

```text
run 직전
    여러 PHC/system pre-post sample 수집
    read interval이 가장 작은 유효 sample 선택

measurement run
    calibration thread는 hot path와 contention하지 않도록 중지하거나
    housekeeping CPU에서 매우 제한적으로 실행

run 직후
    같은 방식으로 sample 수집

offline
    측정 시각을 bracket하는 before/after selected offset을 선형 보정
```

현재 calibrator는 extended ioctl의 clock id를 `CLOCK_MONOTONIC`으로 요청한다.
최종 분석에서 bracket 범위 밖 timestamp는 기본 metric에서 제외한다.

### 14.4 PTP_SYS_OFFSET_PRECISE

`PTP_SYS_OFFSET_PRECISE`는 driver가 hardware cross timestamp callback을 제공해야
한다. 현재 일반 macb PTP 구현은 `gettimex64` 기반으로 보이므로 precise ioctl이
지원되지 않을 가능성이 높다.

`PTP_SYS_OFFSET_PRECISE`가 반환하는 system timestamp domain을
`CLOCK_MONOTONIC`과 동일하다고 가정할 수 없으므로 EXTENDED의 단순 fallback으로
사용하지 않는다. 현재 primary 구현은 `PTP_SYS_OFFSET_EXTENDED`와
`CLOCK_MONOTONIC` 조합만 사용한다.

Precise 지원을 추가하기 위해 별도의 MAC/SoC cross timestamp driver 개발을 이번
초기 범위에 포함하지 않는다.

### 14.5 PHC 직접 읽기

Bring-up 단계에서는 `/dev/ptpX`를 POSIX dynamic clock으로 열고 packet 수신 직후
PHC를 직접 읽어 descriptor timestamp와 같은 domain인지 쉽게 확인할 수 있다.

그러나 packet마다 PHC register를 읽는 방식은 syscall, MMIO, driver lock 비용을
userspace timestamp boundary에 추가한다. 최종 성능 측정에서는 빠른
`CLOCK_MONOTONIC`을 사용하고 offline calibration하는 방식을 primary로 한다.

### 14.6 Clock step 금지

측정 중 PHC 또는 system clock에 큰 step correction이 들어가면 latency가 음수
또는 비정상적으로 커질 수 있다.

Run manifest에 다음을 저장한다.

```text
ptp4l state
master offset
frequency adjustment
phc2sys state
clock step 발생 여부
calibration samples
```

Local ingress latency만 측정한다면 sender와 receiver의 epoch가 같을 필요는 없다.
Target 내부 PHC와 target 내부 MONOTONIC 관계만 보정하면 된다. 반면 기존
application TX-to-RX one-way delay도 분석하려면 두 장치의 clock synchronization이
별도로 필요하다.

---

## 15. 실험 비교군 재정리

Kernel 효과와 packet path 효과를 한 번에 바꾸면 원인을 분리하기 어렵다.
다음처럼 두 단계로 나눈다.

### 15.1 Kernel 및 isolation 비교

```text
C0
    Vanilla kernel
    normal UDP socket

C1
    PREEMPT_RT
    normal UDP socket

C2
    PREEMPT_RT
    CPU isolation/affinity
    normal UDP socket
```

### 15.2 동일 RT 환경에서 path 비교

```text
C3
    PREEMPT_RT + isolation
    Native XDP attached
    XDP_PASS

C4
    PREEMPT_RT + isolation
    Native XDP -> CPUMAP[3] -> kernel stack

C5
    PREEMPT_RT + isolation
    Native XDP -> XSKMAP -> AF_XDP copy

C6
    C3/C4/C5 traffic을 동시에 넣는 mixed architecture
```

향후 macb AF_XDP zero-copy가 구현되면 별도 case를 추가한다.

```text
C7
    Native XDP -> XSKMAP -> AF_XDP zero-copy
```

### 15.3 Timestamp instrumentation control

각 XDP case에서 가능하면 다음을 비교한다.

```text
I0
    classifier only

I1
    classifier + timestamp kfunc + data_meta

I2
    classifier + timestamp + measurement map write
```

이를 통해 instrumentation overhead를 정량적으로 설명한다.

---

## 16. 부하 실험 설계

### 16.1 한 번에 하나의 축부터 변경한다

첫 실험에서 CPU load, packet rate, memory pressure, disk I/O를 모두 동시에
바꾸지 않는다.

권장 순서:

```text
1. idle 상태의 path comparison
2. source RX CPU load sweep
3. destination RT CPU load sweep
4. packet-rate sweep
5. background network traffic sweep
6. 마지막에 복합 부하
```

### 16.2 Source CPU와 destination CPU 부하를 구분한다

CPUMAP은 XDP 이후 work를 CPU 3으로 옮긴다. 그러나 다음 부분은 원래 NIC
RX/NAPI CPU에서 실행된다.

```text
IRQ
NAPI
descriptor 처리
initial XDP classifier
CPUMAP enqueue
```

따라서 다음 두 load는 완전히 다른 실험이다.

```text
source RX CPU contention
destination CPU 3 contention
```

둘을 따로 측정해야 CPUMAP이 어느 interference를 줄이고 어느 interference에는
영향을 주지 못하는지 설명할 수 있다.

### 16.3 Packet rate

단순한 CPU utilization 퍼센트뿐 아니라 packet rate를 독립 변수로 둔다.

예:

```text
1 kpps
10 kpps
50 kpps
100 kpps
line-rate에 가까운 overload 구간
```

실제 가능한 범위는 packet 크기, sender 성능과 Pi NIC 상태를 확인한 뒤 정한다.

### 16.4 Queueing과 loss

높은 load에서 packet이 사라졌다면 수신된 packet만으로 latency percentile을
계산하면 결과가 좋아 보일 수 있다.

항상 다음을 함께 보고한다.

```text
offered packets
classified packets
redirect requested packets
delivered packets
sequence gaps
NIC drops
CPUMAP drops
AF_XDP drops
deadline misses
```

---

## 17. 시스템 환경 통제

### 17.1 반드시 기록할 설정

각 run마다 최소 다음 정보를 자동 저장한다.

```text
date/time와 run id
board model
kernel uname -a / uname -v
kernel config hash
kernel driver branch와 commit
project/BPF commit
PREEMPT_RT 활성 여부
boot command line
online CPU 목록
isolated CPU 목록
IRQ affinity
RPS/XPS 설정
cpumap destination와 queue size
process CPU affinity
process scheduling class와 RT priority
CPU frequency governor
현재 CPU frequency
temperature/throttling 상태
NIC link speed/duplex
RX/TX queue 수
offload 설정
EEE 설정
interrupt coalescing 설정
AF_XDP copy/zero-copy 상태
PTP device와 hwtstamp filter
ptp4l/phc2sys 상태
PHC/system calibration sample
packet size/rate/count
CPU/network workload 설정
```

### 17.2 고정하거나 별도 변수로 둘 항목

```text
CPU frequency scaling
thermal throttling
EEE
GRO/LRO
RPS/XPS
irqbalance
interrupt moderation
background services
RT runtime throttling
```

이 항목들은 무조건 특정 값으로 해야 한다는 뜻이 아니라, run마다 값이 바뀌지
않게 고정하고 논문에 기록해야 한다는 뜻이다.

### 17.3 Single RX path 검증

README의 `single RX path`가 정확히 무엇을 의미하는지 target에서 확인한다.

```text
실제 hardware RX queue가 하나인가?
여러 queue 중 실험 traffic이 queue 0 하나에만 들어오는가?
IRQ와 NAPI instance가 몇 개인가?
```

확인에 사용할 수 있는 정보:

```bash
ethtool -l eth0
cat /proc/interrupts
ls /sys/class/net/eth0/queues
```

논문에서는 `하나의 NIC`와 `하나의 hardware RX queue`를 같은 의미로 사용하지
않는다.

---

## 18. Target에서의 단계별 검증 절차

### Phase 0 — 기존 상태 보존

작업 전에 다음을 보존한다.

```text
현재 정상 boot kernel image
현재 정상 module tree
thesis-macb-xdp branch commit
현재 .config
기능 검증 로그
기존 XDP_PASS/CPUMAP/AF_XDP 실행 결과
```

Kernel patch는 작은 commit으로 나누고 각 단계가 boot 가능한 상태를 유지한다.

### Phase 1 — 기존 SKB hardware timestamp 검증

Driver XDP callback을 추가하기 전에 기존 macb PTP 경로가 일반 UDP packet에
timestamp를 제공하는지 확인한다.

확인 항목:

```bash
ethtool -T eth0
ls -l /sys/class/net/eth0/device/ptp 2>/dev/null
ls -l /dev/ptp*
```

HWTSTAMP_FILTER_ALL을 설정하고 일반 UDP packet을 XDP 없이 또는 XDP_PASS로
수신하여 `SO_TIMESTAMPING_RAW_HARDWARE` 값이 non-zero인지 확인한다.

성공 조건:

```text
UDP 9000/9001/9002 같은 일반 data packet에 hardware RX timestamp가 존재
timestamp가 packet sequence 순서에서 합리적으로 증가
driver warning이나 invalid descriptor가 없음
```

### Phase 2 — 공용 descriptor timestamp helper

기존 SKB timestamp code를 공용 helper로 분리한다.

성공 조건:

```text
XDP 미부착 상태의 socket hardware timestamp regression 없음
PTP traffic regression 없음
timestamp disabled일 때 기존 동작 유지
```

### Phase 3 — xdp_metadata_ops

Driver-private XDP context와 `.xmo_rx_timestamp`를 등록한다.

성공 조건:

```text
kernel build 성공
boot 성공
normal RX 성공
Native XDP PASS/DROP/REDIRECT regression 없음
netdev metadata capability에서 RX timestamp 지원 확인
```

### Phase 4 — 최소 metadata XDP test

처음부터 전체 classifier를 바꾸지 말고 최소 program으로 callback을 확인한다.

```text
timestamp kfunc 호출
성공/error counter 기록
XDP_PASS
```

성공 조건:

```text
timestamp enabled -> success 증가
timestamp disabled -> ENODATA 또는 정의한 error 증가
kernel panic/verifier error 없음
```

### Phase 5 — AF_XDP metadata 전달

Initial XDP에서 `data_meta`를 할당하고 XSKMAP으로 redirect한다.

AF_XDP receiver에서 다음을 검증한다.

```text
magic 일치
version 일치
size 일치
HWTS_VALID 설정
sequence가 packet payload와 일치
timestamp가 0이 아님
timestamp 순서가 합리적
```

Copy mode에서도 metadata가 정상 전달되는지 먼저 확인한다.

### Phase 6 — CPUMAP metadata 전달

`xdp/cpumap` program을 연결한다.

성공 조건:

```text
initial metadata magic/version 확인 성공
hw_rx_ns가 유지됨
initial_xdp_ns가 유지됨
cpumap_ns가 CPU 3에서 기록됨
bpf_get_smp_processor_id() == 3
최종 UDP socket에 packet 도착
flow_id + sequence로 세 record 결합 성공
```

### Phase 7 — Mixed traffic

동시에 다음 packet을 보낸다.

```text
PASS traffic
CPUMAP traffic
XSK traffic
```

성공 조건:

```text
각 sequence가 의도한 path record에만 존재
metadata path field와 실제 consumer가 일치
모든 drop/error counter 설명 가능
background ARP/PTP traffic이 실험 count에 섞이지 않음
```

### Phase 8 — Clock calibration

PHC와 MONOTONIC sample을 run 전후에 수집한다.

성공 조건:

```text
변환 후 대부분의 local latency가 음수가 아님
before/after offset drift가 설명 가능한 범위
clock step 검출 없음
동일 packet의 socket/XDP hardware timestamp가 일치
```

### Phase 9 — Measurement receiver

Packet별 출력 없는 AF_XDP/UDP receiver로 교체한다.

성공 조건:

```text
예정 sample 수 완주
hot path allocation 없음
UMEM frame 누수 없음
종료 후 결과 저장 성공
sequence duplicate/gap 집계 성공
```

### Phase 10 — Load sweep

Idle부터 시작해서 CPU load와 packet rate를 하나씩 증가시킨다.

성공 조건:

```text
각 run 설정 manifest 존재
raw record와 processed result 분리 보존
mean/p50/p95/p99/p99.9/max 계산
loss와 deadline miss 함께 계산
독립 run 반복
```

---

## 19. 기능 및 회귀 테스트 목록

### 19.1 Driver 기본 기능

```text
interface up/down 반복
module unload/load가 가능한 구성이라면 반복
link down/up
MTU 변경
XDP attach/detach 반복
timestamp enable/disable 반복
normal ping/UDP/TCP
```

### 19.2 XDP action

```text
XDP_PASS
XDP_DROP
XDP_REDIRECT to CPUMAP
XDP_REDIRECT to XSKMAP
XDP_TX
ndo_xdp_xmit 사용 경로
```

Timestamp callback 추가가 각 buffer ownership 경로를 망가뜨리지 않아야 한다.

### 19.3 Timestamp negative test

```text
RX timestamping disabled
timestamp filter가 PTP-only
timestamp valid bit 없음
metadata headroom 부족
metadata ABI version mismatch
sequence 없는 packet
잘린 Ethernet/IP/UDP packet
```

### 19.4 시간 경계 test

Descriptor가 저장하는 seconds bit 폭이 전체 PHC seconds보다 작다면 wrap 근처
복원 오류가 없는지 장시간 test 또는 controlled clock test로 확인한다.

검사:

```text
timestamp가 갑자기 과거로 이동하지 않는가
초 경계에서 약 1초 jump 오류가 없는가
PHC adjustment 중 잘못된 upper seconds가 붙지 않는가
```

### 19.5 고부하 test

```text
CPUMAP queue full
AF_XDP RX ring full
AF_XDP FILL ring 부족
NAPI budget 소진
CPU 3 RT task contention
source RX CPU contention
mixed traffic overload
```

---

## 20. 통계 분석 규칙

### 20.1 반드시 보고할 값

```text
sample count
valid timestamp count
loss count와 ratio
invalid metadata count
mean
median/p50
p95
p99
p99.9
observed maximum
standard deviation 또는 명시적으로 정의한 jitter
deadline miss count와 ratio
```

### 20.2 Jitter 정의

`jitter`라는 단어만 쓰지 말고 계산 방식을 명시한다.

예:

```text
latency standard deviation
또는
inter-percentile range p99 - p50
또는
successive latency difference의 absolute mean
```

하나를 primary로 정하고 다른 값은 보조 지표로 둔다.

### 20.3 p99.99 주의

200,000 sample에서 p99.99 바깥쪽 sample은 약 20개뿐이다. 따라서 p99.99를
주요 결론으로 사용하려면 sample 수와 반복 run을 늘리고 신뢰구간을 함께
제시해야 한다.

### 20.4 Maximum의 표현

관측된 최대값은 수학적으로 보장된 WCET가 아니다. 다음처럼 표현한다.

```text
observed maximum latency during the measurement interval
```

### 20.5 반복 실험

각 조건을 한 번만 실행하지 않는다.

권장:

```text
warm-up
동일 조건의 독립 run 여러 회
run 순서 randomization 또는 순서 효과 기록
run별 percentile
aggregate confidence interval
```

---

## 21. Controller 구현 상태와 남은 것

현재 `monitor/controller.sh`는 timestamp+PID의 고유 `run_id` 디렉터리를 만들고,
`ACE_RESULT_ROOT`/`ACE_INTERFACE`를 받아 CPU/network load와 monitoring을
실행한다. 시작 시 `capture_manifest.py`가 repository HEAD/status/dirty patch,
artifact hash, boot/CPU/NIC/PTP/tool 정보를 overwrite 없이 저장한다. 또한
기본 `rpi-6.18.y` merge-base와 이후 exact commit/`format-patch --binary` series,
tracked diff와 untracked source를 포함한 raw binary worktree patch/hash, source/boot
kernel image·DTB·overlay·설치 module tree의 SHA-256을 함께 수집한다.

다음 중 system configuration/manifest와 workload/monitoring 부분은 구현됐고,
loader/receiver/sender/calibration orchestration과 종료 validation은 남아 있다.

```text
preflight
    -> system configuration 수집
    -> clock/PTP 상태 확인
    -> NIC/XDP 상태 확인

setup
    -> hwtstamp filter 설정
    -> loader 실행
    -> map 준비 확인
    -> receiver 실행
    -> process affinity/priority 확인

run
    -> workload 시작
    -> sender 시작 신호
    -> 설정된 sample 수 또는 duration 대기

stop
    -> sender/receiver 정상 종료
    -> BPF records dump
    -> NIC/XDP/drop stats 수집
    -> calibration after sample
    -> loader 종료

validate
    -> expected/received count 확인
    -> missing result 탐지
    -> manifest complete 표시
```

### 21.1 Run directory 예

```text
results/<run-id>/
    manifest.json
    kernel.txt
    cpu.txt
    irq.txt
    nic.txt
    phc-monotonic-before.csv
    phc-monotonic-after.csv
    xdp-ingress_records.csv
    xdp-cpumap_records.csv
    xdp-stats.csv
    xsk-user-records.csv
    udp-samples.bin
    joined.csv
    summary.json
    summary.json.complete
    afxdp_stats.txt
    bpf_stats.txt
    nic_stats_before.txt
    nic_stats_after.txt
    mpstat.log
    network.log
    workload.log
    validation.txt
```

Raw data는 분석 script가 다시 실행되어도 덮어쓰지 않는다.
반복-run aggregate CSV/JSON/completion marker는 여러 run 중 하나에 속하지
않으므로 `results/<condition>/` 같은 별도 조건 디렉터리에 저장한다.

---

## 22. 구현 변경을 나눌 권장 commit 순서

현재 workspace에는 아래 Commit 1~9에 해당하는 소스 변경이 아직 commit되지 않은
상태로 함께 존재한다. 실제 이력을 만들 때는 검토 가능한 단위로 나누고, 각
commit의 실장치 검증 여부를 message나 manifest에 구분해서 남긴다. Manifest-bound
단일-run join/summary/completion과 반복-run aggregation/deadline/Student-t CI는
구현됐고, controller의 전체 XDP/receiver/sender/calibration 자동화와 실제
반복 실험 수집은 남아 있다.

### Commit 1 — 공용 timestamp helper

```text
macb 기존 SKB timestamp code refactor
동작 변경 없이 공용 descriptor-to-ns helper 생성
SKB timestamp regression test
```

### Commit 2 — macb XDP RX metadata callback

```text
driver-private xdp_buff wrapper
xmo_rx_timestamp callback
xdp_metadata_ops 등록
capability/error 처리
```

### Commit 3 — 최소 kfunc test

```text
device-bound XDP load
timestamp success/error counters
XDP_PASS로 기능 검증
```

### Commit 4 — Metadata ABI와 initial classifier

```text
BPF/metadata.h
data_meta reserve/init
sequence/path/timestamps 저장
기존 classifier와 결합
```

### Commit 5 — Loader lifecycle

```text
open -> set ifindex/flags -> load 순서
hwtstamp config
map sizing
Native mode log 정리
safe attach/detach
```

### Commit 6 — AF_XDP metadata receiver

```text
metadata bounds/ABI validation
address helper 정리
no-log measurement buffer
reliable frame recycle
copy mode 명시
```

### Commit 7 — CPUMAP remote program

```text
struct bpf_cpumap_val
xdp/cpumap program
CPU 3 timestamp
cpumap_records map
```

### Commit 8 — UDP receiver와 offline join

```text
CLOCK_MONOTONIC user timestamp
flow_id + sequence 기반 result
ingress/cpumap/user record join
validity/loss report
```

### Commit 9 — Clock calibration

```text
PTP_SYS_OFFSET_EXTENDED + CLOCK_MONOTONIC
최소 system-span sample 선택
before/after samples
offline conversion
```

### Commit 10 — Experiment controller와 문서

```text
run manifest
system setting capture
quiet measurement lifecycle
comparison matrix
README 정확성 정리
```

각 commit마다 가능한 최소 기능을 Raspberry Pi에서 확인하고 다음 단계로 간다.

---

## 23. 현재 수정된 파일과 남은 범위

외부 Ethernet 없이 반영 가능한 source 변경은 다음 파일에 구현되어 있다.

### Kernel workspace

```text
drivers/net/ethernet/cadence/macb.h
drivers/net/ethernet/cadence/macb_main.c
drivers/net/ethernet/cadence/macb_ptp.c
```

### Application/BPF workspace

```text
BPF/metadata.h                 XDP metadata/map/userspace record ABI
BPF/config.h                   metadata/map/sample 설정
BPF/xdp_kern.c                 kfunc/data_meta/cpumap program
BPF/xdp_loader.c               device-bound load/hwtstamp/cpumap/CSV export
BPF/afxdp_recv.c               metadata 수신 및 측정 hot path
BPF/Makefile                   새 source dependency

include/ace_packet_abi.h       BPF/UDP 공용 32-byte wire ABI
include/ace_hwtstamp.h         netdev HWTSTAMP 저장/설정/안전 복구
udp_socket/include/protocol.h  56-byte UDP sample record ABI
udp_socket/src/sender.c        flow ID, 공용 header, DF/MTU 제한
udp_socket/src/receiver.c      monotonic/realtime/HW timestamp와 validation
udp_socket/src/phc_calibrate.c PHC↔MONOTONIC calibration raw CSV
udp_socket/Makefile            새 source/header dependency
Makefile                       BPF+UDP 통합 build와 offline test target
tests/                         wire ABI/CLI/UDP loopback offline regression
analysis/join_results.py       manifest-bound join/clock 변환/semantic validation
analysis/aggregate_runs.py     반복 run deadline/percentile/Student-t CI
analysis/README.md             completion marker와 반복 run 분석 절차
analysis/tests/                join/aggregation analyzer unit test

monitor/controller.sh          고유 run directory, manifest, 부하/monitoring
monitor/capture_manifest.py    exact kernel series, dirty binary patch, installed artifact manifest
README.md                      Native/copy mode/측정 방법
udp_socket/README.md           UDP ABI/실행/clock 설명
```

Controller의 전체 XDP/receiver/sender/calibration lifecycle과 종료 validation은 아직
남은 구현 범위다. 반복-run aggregation/deadline/CI와 exact committed kernel
series, dirty binary patch, source/boot image·DTB·overlay·설치 module tree hash capture는
구현됐지만, 실제 반복 wire run과 설치 artifact가 해당 source로 build됐는지의
운용 검증은 남아 있다.

다음 generated file은 직접 편집하거나 commit하지 않는다.

```text
BPF/vmlinux.h
BPF/xdp_kern.o
BPF/xdp_kern.skel.h
BPF/xdp_loader binary
BPF/afxdp_recv binary
```

---

## 24. 다음 작업자가 반드시 확인해야 할 target 정보

새 kernel을 설치하고 wire/runtime 검증을 시작하기 전에 다음 출력과 source
정보를 확보한다.

```text
uname -a
uname -v
cat /sys/kernel/realtime
cat /proc/cmdline

ethtool -i eth0
ethtool -T eth0
ethtool -l eth0
ethtool -k eth0
ethtool --show-eee eth0

ls -l /dev/ptp*
cat /sys/class/ptp/ptp*/clock_name

bpftool net list
bpftool feature probe kernel

cat /proc/interrupts
ls /sys/class/net/eth0/queues

ps -eLo pid,tid,cls,rtprio,pri,psr,comm
```

Kernel source에서는 최소 다음 symbol을 찾는다.

```text
gem_xdp_run
xdp_init_buff
xdp_prepare_buff
bpf_prog_run_xdp
gem_ptp_do_rxstamp
gem_ptp_rxstamp
gem_hw_timestamp
macb_ptp_desc
macb_dma_ptp
DMA_RXVALID
gem_rx 또는 page-pool RX 함수
net_device 초기화 지점
```

---

## 25. 다음 작업자용 현재 상태 요약

현재 handoff 기준은 다음과 같다.

```text
이 프로젝트는 Raspberry Pi 5의 Cadence GEM/macb driver에 Native XDP를 manual
porting한 PREEMPT_RT packet-processing 연구다.

Driver source에는 공용 GEM descriptor-to-ns helper, driver-private xdp_buff,
xmo_rx_timestamp, metadata-valid XDP buffer, XDP_PASS metadata 보존,
RP1 공용 TX kick, Native-XDP multi-descriptor DROP/counter가 구현되어 있다.

BPF source에는 device-bound Native loader, 56-byte data_meta, flow_id+sequence
HASH records, CPUMAP remote program, 21개 stats, 안전한 attach/detach와 종료 CSV
export가 구현되어 있다. 기본 record capacity는 262,144다.

AF_XDP receiver는 XDP_COPY mode이며 UMEM address/recycle 검증, metadata decode,
packet별 출력 없는 preallocated record, 종료 CSV export를 구현했다.

UDP sender/receiver와 BPF는 32-byte ace_experiment_header를 공유한다. Packet key는
flow_id+sequence이고 run_id는 results/<run-id>/와 manifest에만 둔다. IPv4 MTU
1500, UDP payload 최대 1472, no-fragment, single-descriptor가 실험 범위다.

PHC↔CLOCK_MONOTONIC calibration 도구, manifest-bound 단일-run full outer
join/semantic validation/completion marker, 반복-run deadline/percentile/Student-t CI도
구현되어 있다. 아직 하지 않은 것은 새 kernel boot, 실제 HWTSTAMP/kfunc 동작,
PASS/CPUMAP/XSK 통합, jumbo DROP counter,
XDP_TX/ndo_xdp_xmit, 장시간 UMEM/packet conservation을 외부 Ethernet으로
검증하는 일과 controller의 전체 lifecycle, 실제 반복 run 수집, class별
deadline 조정과 instrumentation on/off 대조 실험이다.

코드 구현 완료와 wire/장시간 검증 완료를 혼동하지 않는다. 생성된 vmlinux.h,
skeleton, object, binary를 직접 편집하지 않으며 기존 사용자 변경도 보존한다.
```

---

## 26. 구현 완료 판정 기준

다음 항목이 모두 충족되어야 timestamp 전달 기능을 완료로 본다.

### Driver

```text
GEM RX descriptor의 실제 timestamp를 반환한다.
xdp_metadata_ops가 capability가 있는 device에만 등록된다.
timestamp disabled/invalid 상태가 명확한 errno로 구분된다.
XDP 미부착 SKB timestamp가 regression 없이 동작한다.
Native XDP PASS/DROP/REDIRECT/TX가 regression 없이 동작한다.
```

### Initial XDP

```text
device-bound kfunc call이 verifier를 통과한다.
data_meta ABI가 bounds-safe하다.
hardware timestamp와 sequence/path가 기록된다.
metadata 실패가 counter 및 flags로 관찰된다.
```

### CPUMAP

```text
CPU 3 remote program에서 initial data_meta가 보인다.
hardware timestamp가 redirect 전후 동일하다.
cpumap_ns와 actual CPU ID가 기록된다.
최종 UDP receiver record와 flow_id + sequence join이 된다.
```

### AF_XDP

```text
copy mode UMEM에서 metadata를 안전하게 읽는다.
magic/version/size를 검증한다.
frame recycle 누수 없이 예정 sample을 완주한다.
packet별 출력 없이 timestamp record를 저장한다.
```

### Clock 및 분석

```text
PHC와 MONOTONIC calibration raw sample이 보존된다.
clock domain을 직접 빼지 않는다.
negative/unmatched sample의 이유가 집계된다.
latency percentile과 loss/deadline miss가 함께 출력된다.
```

### 재현성

```text
kernel/BPF commit과 모든 runtime 설정이 run manifest에 남는다.
같은 조건의 독립 run을 반복할 수 있다.
raw record를 보존하고 offline 분석을 재실행할 수 있다.
```

---

## 27. 자주 생길 수 있는 오해 정리

### 오해 1 — Native XDP면 driver 수정 없이 hardware timestamp를 읽는다

아니다. Native XDP가 descriptor 가까이에서 실행되더라도 driver가
`xdp_metadata_ops`를 구현하지 않으면 BPF kfunc은 timestamp를 얻을 수 없다.

### 오해 2 — CPUMAP에는 timestamp metadata를 전달할 수 없다

아니다. Initial XDP가 `data_meta`에 복사한 custom metadata는 CPUMAP으로 전달할
수 있다. 자동으로 일반 socket의 `skb_hwtstamps`가 되지 않을 뿐이다.

### 오해 3 — bpf_ktime_get_ns가 hardware RX timestamp다

아니다. 이것은 XDP program이 실행된 software 시각이다.

### 오해 4 — Native XDP이면 AF_XDP도 zero-copy다

아니다. 현재 application은 `XDP_COPY`를 사용한다. Zero-copy는 driver의 별도
지원이 필요하다.

### 오해 5 — Redirect counter와 실제 수신 수는 같다

아니다. Redirect action을 선택한 뒤 queue/ring에서 drop될 수 있다.

### 오해 6 — PHC timestamp와 CLOCK_MONOTONIC을 바로 빼면 된다

아니다. 두 clock domain 사이 offset과 drift를 보정해야 한다.

### 오해 7 — PREEMPT_RT와 CPU 3 지정만으로 deterministic하다

아니다. IRQ/NAPI source CPU, CPUMAP kthread policy, userspace priority, frequency,
queueing, background load와 packet loss까지 통제하고 측정해야 한다.

---

## 28. 참고할 Linux 공식 자료

구현 시 target kernel version의 source와 selftest를 최우선 기준으로 사용한다.

```text
XDP RX Metadata
https://docs.kernel.org/networking/xdp-rx-metadata.html

AF_XDP
https://docs.kernel.org/networking/af_xdp.html

BPF_MAP_TYPE_CPUMAP
https://docs.kernel.org/bpf/map_cpumap.html

Network timestamping
https://docs.kernel.org/networking/timestamping.html

XDP metadata BPF selftest
tools/testing/selftests/bpf/progs/xdp_metadata.c

XDP metadata userspace selftest
tools/testing/selftests/bpf/prog_tests/xdp_metadata.c

PTP userspace API
include/uapi/linux/ptp_clock.h

XDP driver API
include/net/xdp.h
```

온라인 최신 source만 그대로 복사하지 말고 Raspberry Pi target의 정확한
`rpi-6.18.y` 기반 source 및 manual port 상태와 비교해서 적용한다.

---

## 29. 최종 요약

현재 architecture의 방향은 유지한다.

```text
Native XDP
    -> packet classification
    -> CPUMAP / XSKMAP / XDP_PASS
```

현재 workspace에는 다음 layer의 소스 구현을 추가했다.

```text
GEM RX descriptor timestamp
    -> macb xdp_metadata_ops
    -> bpf_xdp_metadata_rx_timestamp
    -> XDP data_meta
```

Path별 최종 확인 방식은 다음과 같다.

```text
AF_XDP
    UMEM packet 앞의 data_meta를 userspace가 직접 읽음

CPUMAP
    CPU 3의 xdp/cpumap program이 data_meta를 읽고 measurement map에 기록

XDP_PASS
    driver의 기존 skb_hwtstamps 경로와 XDP record를 함께 검증
```

마지막으로 `flow_id + sequence`를 기준으로 hardware, initial XDP, CPUMAP,
userspace record를 결합하고 PHC와 MONOTONIC clock domain을 보정한다. `run_id`는
결과 디렉터리와 manifest로 관리한다.

이 구조가 완성된 뒤에야 다음 질문을 정량적으로 답할 수 있다.

> 동일한 embedded NIC ingress에서 mixed traffic을 Native XDP로 분류하여
> CPUMAP 기반 RT kernel path와 AF_XDP userspace path로 분리했을 때, CPU 및
> network load가 증가하는 상황에서 local ingress-to-userspace tail latency와
> deadline miss가 얼마나 개선되는가?

기능 검증, timestamp 전달 검증, clock 검증, 측정 receiver 검증을 먼저 끝내고
성능 결론은 반복 가능한 load experiment 이후에만 내린다.
