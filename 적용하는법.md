Raspberry Pi 5 PREEMPT_RT 커널 적용 절차
1. 빌드 도구 설치
```bash
sudo apt update

sudo apt install -y \
    git build-essential bc bison flex \
    libssl-dev libncurses-dev dwarves libelf-dev
```
---
2. Raspberry Pi 6.18 커널 소스 받기
```bash
cd ~

git clone --depth=1 \
    --branch rpi-6.18.y \
    https://github.com/raspberrypi/linux.git

cd linux
```
현재 Raspberry Pi 저장소에는 `rpi-6.18.y` 브랜치가 있고, Pi 5 공식 빌드 설정은 `bcm2712_defconfig`다.
---
3. Pi 5 기본 설정 적용
```bash
make bcm2712_defconfig
```
Pi 5는 공식적으로 `kernel_2712` / `bcm2712_defconfig` 조합을 사용한다.
---
4. PREEMPT_RT 활성화
```bash
make menuconfig
```
여기서 다음 항목을 선택한다.
```text
General setup
→ Preemption Model
→ Fully Preemptible Kernel (Real-Time)
```
설정을 저장한 뒤 확인한다.
```bash
grep CONFIG_PREEMPT_RT .config
```
정상 결과:
```text
CONFIG_PREEMPT_RT=y
```
---
5. 커널 빌드
```bash
make -j6 Image.gz modules dtbs
```
Raspberry Pi 공식 문서의 64-bit native build 명령과 동일하다.
---
6. 모듈 설치
```bash
sudo make -j6 modules_install
```
---
7. RT 커널 설치
이번 SD 카드는 RT 전용으로 사용하므로 기존 커널 백업 없이 바로 덮어쓴다.
커널 이미지 설치
```bash
sudo cp arch/arm64/boot/Image.gz \
    /boot/firmware/kernel_2712.img
```
DTB 설치
```bash
sudo cp arch/arm64/boot/dts/broadcom/*.dtb \
    /boot/firmware/
```
Device Tree Overlay 설치
```bash
sudo cp arch/arm64/boot/dts/overlays/*.dtb* \
    /boot/firmware/overlays/
```
`README` 파일은 RT 커널 동작에 필요하지 않으므로 복사하지 않는다.
---
8. 재부팅
```bash
sudo reboot
```
---
9. RT 적용 확인
재접속 후 다음 명령을 실행한다.
```bash
uname -a
```
출력에 다음 문자열이 포함되어 있어야 한다.
```text
PREEMPT_RT
```
추가 확인:
```bash
cat /sys/kernel/realtime
```
정상 결과:
```text
1
```
이면 PREEMPT_RT 적용 성공이다.
---
10. cyclictest
`rt-tests`를 설치한다.
```bash
sudo apt install -y rt-tests
```
테스트 실행:
```bash
sudo cyclictest -S -m -p98 -i250
```
출력되는 다음 latency 값을 확인한다.
`Min`
`Avg`
`Max`
특히 RT 성능 비교에서는 `Max latency`를 주요 지표로 사용할 수 있다.
